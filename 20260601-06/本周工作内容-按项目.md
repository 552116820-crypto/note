# 本周工作内容（按项目归类）· 2026-06-01 ~ 06-06

## 一、智能拓客（本周重点）

本周开发并交付 4 个接口 + 测试 + 文档。

1. **客户分数与客户基本信息接口**（核心）
   - 梳理 CRM 客户主表 + 两张副表的业务逻辑；取得相关表只读权限，拿到客户星级、战略部客户标签字段；理清客户得分计算规则
   - 兼容地图模式重构接口：返回历史成交客户所在行业、支持分页、默认 60km 且可扩大搜索范围
   - 算法方案：针对 CRM 服务器 MySQL 版本低、无空间索引，采用 **SQL 矩形预筛 + Haversine 精算**
   - 缓存：分数不做持久化，先用进程缓存；评估后确认进程缓存满足不了生产规模，需上 Redis
   - AI 协作流程：需求文档 → Claude Code plan mode 设计 → Codex 审查 → 终版 plan → Claude 生成代码
   - 已完成开发、测试，并审查重点代码块
2. **跳过功能（100%）**
   - 新建「用户—客户」状态表，skip 状态客户在下次推荐中沉底
   - 方案设计 + AI 方案审查；属需求文档外的额外接口，下周与产品经理确认
3. **行为时间轴接口（100%）**
   - 整合订单 / 拜访 / 服务记录，时间倒序取前五条作为返回内容
4. **日志功能（100%）**
   - 新建日志表接入服务代码，实时统计接口流量、支撑阶段性审查汇报
5. **接口测试（100%）**：完成边界测试 + 承接能力测试
6. **接口文档（50%）**：仍需申请 https 域名与证书

## 二、客户流失预警 / 数据库迁移

1. 迁移数据库与表：解决 VPN 连接下 MTU 触发上限问题；备份旧数据，新建更通用的数据库（存放后续 AI 工作台数据），迁移表、改表名、加注释
2. 同步修改流失预警源码，调整服务器环境并重新部署

## 三、AI 销售助手平台（基建与规范化）

1. 规范化预研：流失预警 + 智能拓客为平台前两个业务，规划统一本地 / 服务器项目文档、库名表名、注释，并合并业务代码（有余力才启动 → 顺延下周）
2. 容器化：编写并规范 docker-compose.yml、配置 REDIS_URL；本地 Docker Desktop 跑通 Redis，查看容器日志确认缓存生效
3. 部署预研：研究 https 域名 + nginx 反代，为部署到服务器做准备

## 四、CRM 接口对接（支撑智能拓客）

1. 改造拜访记录接口：新增 customer_ids 请求参数（本地已完成）
2. 新建服务记录接口：复用拜访记录接口设计，统一请求体 / 返回体（等待交付）
3. 与数据库负责人对接：理清客户历史成交订单的获取方式，打通最后堵点

## 五、滴普智能体 × 企微机器人 桥接服务

1. 排查接口超时：滴普接口端并发量低（本地 + 服务器同时调用返回慢）；调用端受白名单 IP 限制（开 VPN 不可用），服务器 IP 已加入白名单

## 六、安仔 AI 机器人

1. 参加产品讨论会，听产品经理介绍并提出问题；企微开发功能为盲点，需阅读开发手册，已排期到 7 月

## 七、其他

1. 参加 AI 每周例会，听取 IT 经理分享
2. Cursor 上完成 CRM 需求半自动化实现（配置文件遵循 Claude 设计哲学；输入 / 输出质量待进一步审查）
