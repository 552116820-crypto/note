1.参加AI每周例会，听取it经理的分享，在cursor上完成crm需求半自动化实现，配置文件严格遵守了claude的设计哲学，输入和输出的质量可能还需要进一步审查
2.排查“企微机器人和滴普智能体”的桥接服务中接口超时的问题。滴普接口端：存在并发量低的问题，本地和服务器上同时调用，结果返回会很慢；调用端：滴普存在白名单ip，开vpn用不了，服务器ip已经让刘付总加进白名单ip了
3.梳理“智能拓客”后续工作计划和具体的实现路径，拿到crm数据库相关的表的只读权限，计算客户得分的过程，并初步构造接口的数据结构

1.针对“智能拓客”，“滴普智能体”这两个项目，开展AI团队内部的技术方案讨论，拓宽思路，明确后续实现目标
2.理解复杂的业务逻辑，梳理清楚crm客户信息表，主表+两张副表，用实例验证结果，并确定好需要的字段
3.对接crm维护数据库的负责人，理解数据表的最初设计逻辑，按需求拿到客户星级与战略部的客户标签这两个字段
4.设计“客户分数与客户基本信息”这个接口，并按要求返回json，理清客户得分的计算规则，准备好需要的数据
5.写清楚这个需求的提示词，一个接口写一套提示词，分三个接口，最难的是“客户分数”接口，所需要的字段分散在数据库中，同时还需要理解业务逻辑和建表逻辑


1.重点开展“智能拓客中”的“客户分数和客户基本信息”的接口开发，基本理清了数据库表与表之间的逻辑；对接数据库负责人，明白了客户历史成交订单和客户怎么获取，打通了最后的堵点
2.为了兼容地图模式，重新设计了接口，现在可以返回客户历史成交客户所在的行业，并支持分页，允许扩大搜索范围（默认60km）
3.为了应对crm服务器上MySQL版本低，无法使用空间索引的问题，采纳AI给出的方案：SQL 矩形预筛 + Haversine 精算
4.算出来的分数不在数据库做持久化，因此放在进程的中进行缓存，如果后续有必要可以上Redis
5.写好需求文档，包括需求解释，技术实现，接口规范等细节，给到claude  code plan mode设计方案，再给codex做审查，最终根据codex评审建议与评判，更新出终版plan，给到claude生成项目代码
6.对接crm工程师，提出了需求，要求写个接口，返回行为时间轴所需要的信息


1.开发“客户分数和客户信息”接口，理清字段所在的数据库位置与业务逻辑，返回前端友好的json。对这个接口做好测试并审查重点代码块
2.对接crm工程师，改造拜访记录接口，添加customer_ids请求，已在本地做好；新建服务记录接口，按拜访记录接口设计逻辑，统一请求体和返回体，等待交付。
3.开发推荐客户“跳过”按钮的后端实现逻辑，通过用户状态来判断
4.考量Redis的必要性，对接口返回的“有序候选+分数”是否做Redis进行评估，结果需要Redis缓存，进程缓存满足不了生产环境大范围应用的需求
5.考量AI销售助手平台的项目规范，算上客户流失预警，智能拓客是第二个业务，
规范本地，服务器上的项目文档，规范数据库上的库名和表名，做好注释，合并业务代码，有时间才开始做。
6.开发推荐客户“跳过”按钮的后端实现逻辑，通过用户状态来判断
7.研究部署https域名，nginx反代服务，为后续部署到服务器做准备

1.迁移数据库和表，解决vpn连接的情况下，mtu触发上限的问题。做好旧数据的备份，新建数据库，更通用，存放后续AI工作台数据，迁移表，改表名，添加注释。同步修改流失预警源码，同时修改服务器上的环境并重新部署
2.添加Redis缓存，编写docker-compose.yml并规范设计，配置REDIS_URL 环境变量行，做好Redis缓存生效的验证。启动本地docker desktop，跑通Redis，查看容器日志，确定Redis生效
3.开发“跳过功能”，做方案设计与ai方案审查，开发新接口，新建用户状态表对“跳过客户”做筛选
4.开会，就“安仔AI机器人”做讨论，听产品经理做介绍，提出问题，企微开发功能是盲点，需要阅读开发手册，并安排到7月来做
5.承接crm接口，新建的服务记录接口，为后续行为时间轴接口开发做准备


1.为智能拓客开发“跳过”功能，允许用户在客户页面点击跳过，在下次推荐客户时，不优先推荐已经“跳过”的客户，原理是在数据库中建立用户与客户之间的状态，状态为skip的客户就会在推荐结果中沉底。额外开发的接口，需求文档没说清楚，下周和产品经理确认。开发已完成，进度100%。
2.承接crm行为时间轴信息接口，按照需求文档说明，整理订单，拜访，服务记录，时间倒叙排列并取前五条信息，作为行为时间轴接口的返回内容。开发已完成，进度100%
3.开发“日志功能”，为了能实时统计接口流量，以及阶段性审查做汇报，日志必不可少，新建日志表接入服务代码实现。开发已完成，进度100%
4.测试接口，做好边界测试与承接能力测试，确保上线后能稳定工作。测试进度50%，等正式域名上线还需要测试
5.编写接口文档，仍需申请https域名和证书，进度50%。

