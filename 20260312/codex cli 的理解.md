你是怎么执行命令，用的powershell，cmd语法吗
unix


• 我这里当前不是用 PowerShell，也不是 cmd，而是用 bash 执行命令。

  从当前环境看：

  - shell: bash
  - 工作目录: /mnt/e/mysql_connect

  所以更像是在类 Linux / WSL 的终端里操作。
  我平时做事主要有几种方式：

  - 执行命令：通过 bash 跑终端命令
  - 改文件：直接打补丁修改代码
  - 查项目：用 rg、ls、git 这类命令
  - 如果需要网页调试：可以用浏览器 DevTools 工具

  如果你需要，我也可以把命令改写成 PowerShell 或 cmd 版本给你。

