• 这次输出已经说明了真实状态：卸载没有完成。

  关键信号是：

  - LeaderTerm、LeaderTermDaemon、Ldcore、LdCdRomFilters 已经被卸载程序删掉了，所以 sc query 才会报 1060
  - ldwfp 还在 STOP_PENDING
  - LdMFilter 还在 RUNNING
  - LdTerm.exe、LdApproval.exe、LdTermPlug*.exe、LdMsgInject*.exe 还活着

  所以现在最稳的做法不是继续硬停，而是先重启。LdMFilter 这种启动级过滤驱动基本就该靠重启卸下。

  我已经把桌面脚本修好了，不再有 kill_user_processes 的问题，也不会再让你手工“猜”卸载是否成功：
  /mnt/c/Users/55211/Desktop/remove_tipray_admin.bat

  你现在这样做：

  1. 先重启电脑。
  2. 重启后用“管理员身份”打开 CMD 或 Windows Terminal。
  3. 运行：

  C:\Users\55211\Desktop\remove_tipray_admin.bat

  4. 这次 Try official uninstall first? 直接选 N。
  5. 如果它提示 Leftovers are still present.，选 Y 做强制清理。

  如果你不想等重启，也可以先手工看一眼当前残留：

  sc query ldwfp
  sc query LdMFilter
  tasklist | findstr /i "LdTerm LdApproval LdFileGate LdMsgInject LdTermPlug LdTermDaemon"

  重启后把新脚本的输出贴给我，我继续帮你收尾。



• 还没算“删干净”，但已经算“功能上卸掉了”。

  现在的状态是：

  - 没有运行中的绿盾进程。我查了 LdTerm、LdApproval、LdFileGate、LdMsgInject、LdTermPlug，都没在跑。
  - 主服务/驱动注册基本没了。LeaderTerm、LeaderTermDaemon、Ldcore、LdCdRomFilters、LdMFilter 都查不到了。
  - 当前也没有这些驱动被系统实际加载。driverquery 没有命中它们。
  - LdTerm 主程序目录也没了，C:\Inetpub\ftproot\Tipray 下面只剩备份/临时/策略库之类的残留，没有 LdTerm.exe 这类可执行文
    件。

  还剩的残留有：

  - 一个已禁用的驱动注册：HKLM\SYSTEM\CurrentControlSet\Services\ldwfp
    它现在是 STOPPED，而且 Start=4，不会再自启动。
  - 残留目录：/mnt/c/Inetpub/ftproot/Tipray
  - 残留驱动文件：/mnt/c/Windows/System32/Drivers/LdMFilter.sys
  - 残留驱动文件：/mnt/c/Windows/SysWOW64/Drivers/LDMFilter.sys
  - 残留驱动文件：/mnt/c/Windows/SysWOW64/Drivers/LdCdRomFilters.sys

  所以结论是：绿盾终端已经不在工作了，但系统里还有少量“尸体”没清掉。
  如果你要，我下一步可以直接给你一套“最后清残留”的管理员命令，把 ldwfp 注册项、Tipray 目录和这几个 .sys 文件一起收尾。
