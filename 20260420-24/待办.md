1. 客户流失预警，技术文档，接口文档，业务调整，配置接口，优化性能，拉取数据，检测算法和落库逻辑
	1. ~~流程与性能能否优化
	2. ~~触发 DeepSeek AI   分析派发，去重，分析过的就不要再分析了;帮我添加提示词，有些拜访记录太久远了就不做上下文考虑了，有效拜访记录时间应当是最后下单日期的半年前到现在
	3. ~~法定节假日只关注春节长假，其余的不再考虑
	4. ~~接口请求体的customer_id字段要不要换成字符串，和crm接口统一
	5. ~~分析技术实现，写技术文档
	6. ~~自动处理旧预警
	7. ~~确认最后下单时间，frequency类型只看最后一次下单时间，针对最后下单时间距离现在一个月以上的客户，一个月内的有下单的客户就不做预警~~
	8. ~~接口给谁，下一步对接~~
	9. ~~http和https
	10. ~~volume的下单金额，接口返回的字段
	11. ~~后台配置的问题，先关闭AI分析

~~全局配置的接口，点击更新，全量重算
PUT http://8.138.104.14:8080/api/warning-configs/global~~

~~/api/warnings这个接口返回的字段
对于frequency和product类型的预警，previous_month_order_amount和days_since_last_customer_order这两个字段没必要；对于volume类型的预警，last_order_date和predicted_cycle_days这两个字段没必要出现~~

~~再次review这个项目代码，重点关注修改的部分，确保和之前版本的兼容，验证配置接口可用，确认文档补齐完整~~

~~评价一下这个项目代码的性能，避免高复杂度，导致真实大量数据运行缓慢，还要保证后期维护不太困难

~~删光数据库的数据，然后再进行全量测试

~~真实 CRM + MySQL 的全量烟测，

现在我这个项目代码准备上传到gitlab，然后部署到服务器上了，帮我看一下，一切准备就绪了没有

2. 需求确认：
   1\. ~~CRM H5 做配置入口~~
   2\. ~~低中高风险判断依据需要确认~~
   3\. ~~对于volume只看环比的下单数量就判断，有些片面，因为如果产品使用周期是两个月，那你用环比就不合适了啊，环比月份没什么意义~~

   4\. 代写日志，业务员真的每天都要写吗？为什么我看到写了拜访记录就行了


3. 复盘这次客户流失预警功能的开发
	1. 工作内容没确定好，需求文档感觉还是有模糊的地方
	2. 工作职责边界没定好，谁做哪一块没有进行沟通，没有调度好
	3. 交付物的归属也没说清楚，接口和数据库
	4. 模型的上下文窗口不够怎么办，每次需要重开一次会话或压缩内容感觉都有些不舒服
