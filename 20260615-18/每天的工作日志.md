1.对接前端工程师，交付本地测试接口，在企微共享文档中编写好接口文档，规范格式，并且对接口逐个做自连测试，验证本机ip的base_url接口能被同一局域网的请求到。完成进度100%。
2.接口返回的字段缺少联系人和联系方式，需求文档并未提及，但html有，补充接口字段信息，同步日志，在合并项目中也做同步。完成进度100%。
3.多个维度评估项目的代码，运维安全方面仍不成熟，清掉过时 editable 安装，添加churn CRM 取数/解析层的保险兜底，现在代码仅运行业务已完善。完成进度50%。
4.了解dify部署情况，nginx配置，端口占用情况，发现dify容器的docker-nginx已占用443端口，需要把它解耦出来，让443端口分发服务，使得https域名不显式端口。完成进度80%。


1.docker部署ai销售系统，包含智能拓客，流失预警，后续功能待开发，已上线服务器，做好灰度测试，接口测试，配置好.env，距离正式接口上线只差配置nginx
。完成进度80%。
2.对接服务器负责，请求把aisales.amer.cn做dns解析，并签发证书，dns解析已经做好，等待证书签发文件。完成进度50%。
3.因为dify占用了443/80端口，https域名暂时放在dify nginx网络下面，时间充足可进行解耦操作。完成进度50%。
4.排查dify查客户功能，存在大模型幻觉，没有信息依据，即没有检索结果的客户会返回内容。完成进度100%。
5.开发企微机器人桥接滴普智能体服务，现在支持多个企微机器人配置。完成进度100%。


1.学习运维相关知识，对接服务器负责人，拿到aisales的https域名证书，准备配置nginx，审查方案和步骤。完成进度50%。
2.修复ai推荐理由的bug，添加现有时间加进提示词中，做时间判断。完成进度100%。
3.候选客户上限的问题，设置成30000个，确认Redis缓存方案。完成进度100%。
4.规范log表cost_ms的数值，现在支持亚毫秒级，因为有些请求走的缓存，热路径，零点几毫秒会显示0。完成进度100%。
5.整理sql迁移文件。完成进度100%。
6.排查amer_sales_assist日记记录没写进log表的原因。完成进度100%。
7.排查log表的异常日志情况，修复测试内容写进日志表的bug。完成进度100%。


1.上线接口Base URL：https://aisales.amer.cn，在服务器上配置好.pem,.key，接入dify 的 docker网络，重写nginx配置，写docker-compose.override.yml做好网络配置持久化。完成进度100%。
2.了解解耦dify 443端口的方案，现在dify占据https总入口，aisales寄生在dify网络下。期望是dify.amer.cn 和 aisales.amer.cn都走443端口，需要一个统一的 443 入口、按域名分流、URL 不带端口。完成进度20%。
3.尝试对企微机器人回复气泡实现图文混排，经真实测试，目前正在使用的长连接方式在流式输出文本的同时，不能在同一个气泡窗口返回图片，只能单开气泡发送图片，若使用markdown消息格式，虽然能实现图文混排，但一次性返回内容，还会返回思考内容，且等待时间很长，体验很差。完成进度100%。
4.阅读企微开发文档，官方文档有说明使用短连接的方式，可以把图片放在流式输出的末尾，但要实现短连接需公网域名，代码改造，多bot的场景架构也需改造。完成进度20%。
5.对接前端工程师，智能拓客线上接口已上线，同步做好接口的测试，看后续的反馈，没问题的话可以上线功能。完成进度80%。