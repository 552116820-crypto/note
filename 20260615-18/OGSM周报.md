# OGSM 周报

## 本周总结

| 分类 | 指标说明 | 关键举措 | 复盘与思考 |
|---|---|---|---|
| 策略S | 推进 AI 销售系统（智能拓客、流失预警）研发与前端对接，完善代码运维安全、企微机器人能力及相关 Bug 修复 | 1. 对接前端工程师，交付本地测试接口，于企微共享文档编写并规范接口文档，逐个接口自连测试，验证局域网内可请求本机 IP 的 base_url 接口；补充接口缺失的联系人、联系方式字段，并同步日志与合并项目（100%）。<br>2. 多维度评估项目代码，清理过时 editable 安装，为 churn CRM 取数/解析层添加兜底保险，当前代码仅运行业务已完善（50%）。<br>3. 修复 dify 查客户功能的大模型幻觉（无检索结果的客户不再返回内容，100%）；修复 AI 推荐理由 Bug，将当前时间加入提示词做时间判断（100%）。<br>4. 开发企微机器人桥接滴普智能体服务，支持多企微机器人配置（100%）。<br>5. 候选客户上限设为 30000 并确认 Redis 缓存方案；规范 log 表 cost_ms 数值，支持亚毫秒级（解决缓存热路径请求显示 0）；整理 SQL 迁移文件；排查 amer_sales_assist 日志未写入 log 表原因，修复测试内容误写入日志表的 Bug（均 100%）。<br>6. 调研企微机器人回复气泡图文混排方案（20%）。 | 1. 代码运维安全仍不成熟，churn CRM 取数/解析层兜底待继续完善（50%）。<br>2. 企微图文混排受限：实测当前长连接方式在流式输出文本的同时无法在同一气泡返回图片，仅能单开气泡发图；markdown 格式虽可图文混排，但一次性返回、含思考内容且等待久、体验差。官方文档说明短连接可将图片置于流式输出末尾，但需公网域名、代码改造及多 bot 架构改造，落地成本高（20%）。 |
| 衡量指标M | AI 销售系统接口上线与服务器部署进展，含 HTTPS 域名、证书、nginx 配置 | 1. docker 部署 AI 销售系统（智能拓客、流失预警，后续功能待开发）至服务器，完成灰度测试、接口测试、.env 配置，距正式接口上线仅差 nginx 配置（80%）。<br>2. 对接服务器负责人完成 aisales.amer.cn DNS 解析并申请证书，已拿到 HTTPS 域名证书。<br>3. 上线接口 Base URL https://aisales.amer.cn：服务器配置 .pem/.key，接入 dify docker 网络，重写 nginx 配置，编写 docker-compose.override.yml 持久化网络配置（100%）。<br>4. 智能拓客线上接口已上线并完成接口测试，待前端反馈无误后可上线功能（80%）。 | 1. dify 容器 docker-nginx 已占用 443/80 端口，aisales 暂寄生于 dify nginx 网络；期望 dify.amer.cn 与 aisales.amer.cn 统一 443 入口、按域名分流、URL 不带端口，解耦方案待推进（20%）。 |

### 本周总结（得与失）

- **得**：AI 销售系统（智能拓客、流失预警）完成 docker 部署并上线服务器，HTTPS 域名 https://aisales.amer.cn 正式上线（证书、nginx、docker 网络配置完成并持久化）；本地测试接口交付前端并完成接口文档与自连测试；修复 dify 查客户幻觉、AI 推荐理由时间判断、log 表 cost_ms 亚毫秒级、测试内容误写日志等多个问题；企微机器人桥接滴普智能体并支持多 bot；候选客户上限设为 30000 并确认 Redis 缓存方案。
- **失/风险**：dify 占用 443/80 端口，aisales 仍寄生于 dify nginx 网络，统一入口与域名分流解耦未完成（20%）；企微图文混排受长连接限制，短连接改造依赖公网域名与多 bot 架构改造，成本高、体验问题未解；churn CRM 取数/解析层兜底未完成（50%），代码运维安全仍不成熟。

## 下周计划

> 注：本节由日志中未完成项（20%/50%）及前瞻性表述推断，原日志无明确"下周计划"。责任人、完成时间日志未提供。

| 分类 | 指标说明 | 关键举措 | 责任人 | 完成时间 |
|---|---|---|---|---|
| 策略S | 完善代码运维安全 | 继续完善 churn CRM 取数/解析层兜底保险（当前 50%） | 未提供 | 待确认 |
| 策略S | 企微机器人图文混排（待确认是否推进） | 评估短连接改造方案：需公网域名、代码改造及多 bot 场景架构改造 | 未提供 | 待确认 |
| 衡量指标M | dify 443 端口解耦 | 推进统一 443 入口、按域名分流、URL 不带端口，使 dify.amer.cn 与 aisales.amer.cn 共用 443（当前 20%） | 未提供 | 待确认 |
| 衡量指标M | 智能拓客功能正式上线 | 跟进前端反馈，确认无误后上线智能拓客功能（接口当前 80%） | 未提供 | 待确认 |
