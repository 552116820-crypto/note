# 本周工作总结（按项目）

## 一、AI 销售系统（amer_sales_assist / aisales｜智能拓客、流失预警）

**部署与上线**
1. docker 部署 AI 销售系统至服务器，完成 .env 配置、灰度测试与接口测试，距正式接口上线仅差 nginx 配置（80%）。
2. 对接服务器负责人，完成 aisales.amer.cn DNS 解析并申请、拿到 HTTPS 域名证书（100%）。
3. 正式上线接口 Base URL https://aisales.amer.cn：配置 .pem/.key，接入 dify docker 网络，重写 nginx 配置，编写 docker-compose.override.yml 持久化网络配置（100%）。

**接口对接（前端）**
1. 交付本地测试接口，于企微共享文档编写并规范接口文档，逐个接口自连测试，验证局域网内可请求本机 IP 的 base_url 接口（100%）。
2. 补充接口缺失的联系人、联系方式字段（需求文档未提及、HTML 已有），并同步日志与合并项目（100%）。
3. 智能拓客线上接口已上线并完成接口测试，待前端反馈无误后可上线功能（80%）。

**业务逻辑与算法**
1. 修复 AI 推荐理由 Bug，将当前时间加入提示词做时间判断（100%）。
2. 候选客户上限设为 30000，确认 Redis 缓存方案（100%）。

**数据与日志**
1. 规范 log 表 cost_ms 数值，支持亚毫秒级（解决缓存热路径请求显示 0 的问题，100%）。
2. 整理 SQL 迁移文件（100%）。
3. 排查 amer_sales_assist 日志未写入 log 表的原因（100%）。
4. 排查 log 表异常日志，修复测试内容误写入日志表的 Bug（100%）。

**代码质量与运维安全**
1. 多维度评估项目代码，清理过时 editable 安装，为 churn CRM 取数/解析层添加兜底保险，当前代码仅运行业务已完善（50%）。

## 二、dify 平台（部署运维与查客户功能）

1. 排查 dify 部署、nginx 配置与端口占用情况，发现 dify 容器 docker-nginx 已占用 443/80 端口。
2. 推进 443 端口解耦方案：目标为统一 443 入口、按域名分流、URL 不带端口，使 dify.amer.cn 与 aisales.amer.cn 共用 443；当前 aisales 暂寄生于 dify nginx 网络下（20%）。
3. 排查并修复 dify 查客户功能的大模型幻觉问题——无检索结果的客户不再返回内容（100%）。

## 三、企微机器人（桥接滴普智能体）

1. 开发企微机器人桥接滴普智能体服务，支持多企微机器人配置（100%）。
2. 调研回复气泡图文混排方案：实测当前长连接方式在流式输出文本的同时无法在同一气泡返回图片，仅能单开气泡发图；markdown 格式虽可图文混排，但一次性返回、含思考内容且等待久、体验差。查阅企微开发文档，短连接可将图片置于流式输出末尾，但需公网域名、代码改造及多 bot 架构改造（20%）。
