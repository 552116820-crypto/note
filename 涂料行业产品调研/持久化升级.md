● 按这次会话的实际做法 + 项目历史归纳，5 个落点，每个性质不同：

  落点: memory/feedback_*.md（auto-loaded）
  写什么: 公司特定站点的踩坑（域名、URL pattern、隐藏字段、SKU 藏哪儿）+ Why + How to apply
  不写什么: 通用流程规则、能从代码/git log 重建的事
  触发条件: 1 家具体公司的坑，未通用化前
  ────────────────────────────────────────
  落点: SKILL.md（subagent 主流程）
  写什么: 硬规则（违反 = 重做）；Phase 内步骤；带公司名+日期的反例教学
  不写什么: 公司专属配置；可执行代码
  触发条件: 多家公司印证后升级为"通用规则"；trace_token 跟着升版
  ────────────────────────────────────────
  落点: references/site-patterns.md
  写什么: 按站点类型分 Pattern（识别 / 分页 / URL / 实测公司 / 模板 / 易踩坑），6 段统一格式
  不写什么: 单家公司的细节
  触发条件: 见到第 N 家用同类型站点，需要"分类抓取手册"
  ────────────────────────────────────────
  落点: templates/template_*.py
  写什么: 可执行 Python 骨架（fetch + 解析 + 落 JSON），TODO 占位让 subagent 改几行就跑
  不写什么: 不能稳定脚本化的（如纯 Read 图片 OCR）
  触发条件: 该类站点抓取逻辑可代码化（正则/翻页参数稳定）
  ────────────────────────────────────────
  落点: CLAUDE.md（主会话索引）
  写什么: 主会话职责（subagent 替不了的活）+ 📚 索引表（一行一规则，指向 SKILL.md/memory/template）
  不写什么: 流程细节、能从 SKILL.md 查到的事
  触发条件: 任何一处持久化完成后，索引表加一行让主会话能查

  这次会话两条坑的落点对照：

  内容: 欧利雅 JZ h-pd 是空壳
  memory: ✓ feedback_gdolivia_jz_template_fake_detail
  SKILL.md: ✓ Phase 4.1 末段
  site-patterns: ✓ Pattern 6
  template: ✓ template_jz_listing.py
  CLAUDE.md 索引: ✓
  ────────────────────────────────────────
  内容: 东洋色材 SKU 全在图里
  memory: ✓ feedback_zhtoyocolor_skus_in_images
  SKILL.md: ✓ Phase 5.1.5
  site-patterns: ✓ Pattern 7
  template: ✗ 故意不做（90% 重合 image_codes）
  CLAUDE.md 索引: ✓

  决策树（下次遇到新坑可参考）：
  1. 只 1 家见到 → memory（不上 SKILL.md，免污染主流程）
  2. ≥2 家见到 + 抓取方式相同 → site-patterns Pattern N（按 6 段格式补一节）
  3. 抓取逻辑能脚本化 → templates/template_*.py（带 4-5 个 TODO 占位）
  4. 违反规则的成本明确（漏行/错列） → SKILL.md Phase 加硬规则（trace_token 升版）
  5. 以上任一动作完成 → CLAUDE.md 索引表加 1 行（不复制内容只写指针）

  反原则（不要做的事）：
  - 同一规则在 SKILL.md 和 CLAUDE.md 各抄一份 → 两处维护早晚漂移
  - 公司专属配置塞进 SKILL.md → subagent 跑别家会被误导
  - 还没第二次复用就先写 template → 维护成本高于收益
  - memory 当成 todo list 用（应该用 TaskCreate）

  ---
  
  这次补全：

  - references/site-patterns.md — 新增 Pattern 8「详情页空 + PDF 产品手册」(~60 行)，含识别特征、PDF
  文字/图片型分支、SKU 正则、黑名单、subagent 并行模式、附录过滤、易踩坑 4 条、实测公司
  - templates/template_pdf_manual.py — 新模板骨架（~250 行），4 个 step：step1_crawl_pdfs / step2_classify_pdfs /
  step3a_parse_text_pdfs（文字型 → SKU 行）/ step3b_render_pages（图片型 → 等派 subagent）
  - SKILL.md Phase 4.6 — 加 2 行指向模板和 Pattern 8 详表
  - CLAUDE.md 工作产物索引 — 模板数从 "4 个" 改成 "6 个" 并补上 jz_listing / pdf_manual（之前 jz_listing
  也漏在索引里了，顺便补）

  整套持久化层级现在统一：

  MEMORY.md → 3 条 feedback（cnscc / yipsink / 通用 PDF 模式）
     ↓
  CLAUDE.md §📚 索引表 → 1 行索引（指向 SKILL.md Phase 4.6 + memory + scripts/lib/pdf.py）
     ↓
  SKILL.md Phase 4.6 → 完整流程（subagent 直接读）
     ↓
  references/site-patterns.md Pattern 8 → 站点判别 + 详细案例
     ↓
  templates/template_pdf_manual.py → 可执行骨架（copy 改 TODO）
     ↓
  scripts/lib/pdf.py + scripts/crawl_*+parse_*+build_* → 实际工作脚本

  trace_token 已升 v9 → v10，CLAUDE.md EXPECTED_TRACE 同步更新。
