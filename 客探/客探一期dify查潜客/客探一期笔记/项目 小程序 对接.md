  - 方式 3（推荐上线做法）：小程序自己做聊天 UI，通过后端调用 Dify API（最可控/最安全）
    你在小程序里做一个原生对话页；小程序请求你的网关服务，网关再去调 Dify 的对话 API。
    这样你可以：
      1. 不把 Dify 的 API Key 暴露在小程序端；
      2. 可靠拿到企业微信的 userid 做权限隔离（“只查自己名下30个客户”）；
      3. 首次使用时由网关触发“拉 CRM → 写入 Dify Dataset”的缓存逻辑；
      4. 更容易做审计、限流、写回 CRM、以及卡片/列表等销售友好组件。
