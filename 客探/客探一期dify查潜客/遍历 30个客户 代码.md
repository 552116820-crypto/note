  可以实现，你只要改成“遍历 data 里的每个企业并拼接输出”。核心改动如下（可直接替换你代码里对应部分）：

  # 原来是只取第一条：
  # company = next(iter(data_map.values()))
  # ...提取...
  # return {"txt": to_semicolon_line(result)}

  # 改成多条：
  companies = []
  if isinstance(data_map, dict):
      # 情况1：data本身就是单个企业对象
      if "customer_base" in data_map or "customer_business_info" in data_map:
          companies = [data_map]
      else:
          # 情况2：data是 {id: 企业对象, id2: 企业对象, ...}
          companies = [v for v in data_map.values() if isinstance(v, dict)]
  elif isinstance(data_map, list):
      companies = [v for v in data_map if isinstance(v, dict)]

  lines = []
  for company in companies:
      # 这里复用你原来的单企业提取逻辑，得到 result(dict)
      # ...
      lines.append(to_semicolon_line(result))
 
  return {"txt": "\n\n".join(lines)}
 
  另外提醒两点（很关键）：

  - 30 家企业拼成一个 txt 可能很长，调用“创建知识库文档 API”时可能被长度限制截断。
  - 更稳的做法是：循环 30 家企业，1 家创建 1 个文档（检索效果也更好）。
 
  如果你要，我可以下一条直接给你一份完整的“30家可用版”代码（输出变量仍然只有 txt），你复制即用。
