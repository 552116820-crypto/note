  不过你当前这个 out.txt（我在本地看了一下）还有几个点会显著影响召回稳定性与模型读懂程度：分块边界是否会被打断、内容结构是否利于检索/阅读、以及编码/字段规范化。
  
  你说你在 Dify 里是“只按 \n\n 分隔，不会再按最大长度二次切分”，这对“一个企业一个块”是最理想的前提。


  - 是否用 Markdown（md）不是关键：对向量检索而言，md 和 txt 都能用；真正决定效果的是“结构化程度 + 企业名/唯一标识是否突出 + 是否有别名/关键字段可匹配”。
  
  - 推荐默认：继续用单文件也行，但把每个企业块改成“标题 + 多行字段（key: value）”的结构（md 或纯文本都可以）。


每个企业块内容从你现在的 "字段":"值";"字段":"值";... 转成更利于检索与模型阅读的 Markdown 多行 key-value + 轻量列表（块内不出现空行）。     



  用同一套问题测试 20 家企业（随机挑）：
 
  1. 仅企业名：“山西阳泉盂县兴发煤业有限公司”
      - 期望：Top1 召回即该企业块。
  2. 企业名 + 字段问题：“山西阳泉盂县兴发煤业有限公司 联系电话是什么？”
      - 期望：召回该块；答案能从“联系电话”字段抽取。
  3. 信用代码查询：“91140000581237507B 是哪家公司？”
      - 期望：召回包含该代码的企业块。
  4. 模糊别名（如有曾用名/简称）：“兴发煤业 盂县 电话”
      - 期望：仍能召回；若失败，补充“别名/曾用名”字段到块开头。
  5. 反例：问不存在企业名
      - 期望：模型能说“未找到匹配企业”，而不是编造（可通过提示词约束）。



  ## 1) 最稳的对应方式：一条联系人 = 一行列表项

  在每个企业块里加一节“联系人”，然后把 customer_contact 数组逐项输出为列表项：
  示例（同一个企业块内）：

  联系人：
  - 姓名：李总｜部门：财务部｜职位：经理｜身份：对账人,财务,采购｜手机：13607232030｜座机：13607232030｜邮箱：-
  - 姓名：lizhens｜部门：供应部｜职位：经理｜身份：-｜手机：13829797123｜座机：-｜邮箱：-

  这就实现了你要的“一一对应”：customer_contact[i] → MD 第 i 条列表项。
  
  ## 2) 字段怎么取：从 customer_contact 的每个对象按固定映射输出
  以你 JSON 里这一条为例（我看到字段有 name/department/position/phone/tel/fax/email/identity 以及一些 is_xxx 标记），建议映射如下：

  - 姓名 ← name
  - 部门 ← department
  - 职位 ← position
  - 身份 ← identity（你这里很有信息量，比如“对账人,财务,采购”）
  - 手机 ← phone
  - 座机 ← tel
  - 传真 ← fax（可选，通常可省略）
  - 邮箱 ← email

  ## 3) 块内不出现 \n\n：两条硬规则
 
  - “联系人：”这一行后面紧跟列表项（不要空一行）。
  - 列表项之间也不要空行。
 
  也就是说，企业块应该像这样连在一起：

  # 企业名称：...
  统一社会信用代码：...
  ...
  联系人：
  - ...
  - ...
  联系电话：




 2. 仅当 customer_contact.length > 0 时才输出

  - 输出一行：联系人：
  - 然后按数组顺序逐项输出（一项对应一行，保持顺序，不去重）：
      - - 姓名：{name}｜部门：{department}｜职位：{position}｜身份：{identity}｜手机：{phone}｜座机：{tel}｜邮箱：{email}
  - 规则细节：
      - 空值（null、""、"-"）按“字段整体省略”或输出 - 二选一；为保证一致性，建议输出 -（更利于模型判断“缺失”）。
      - 不允许插入空行，避免块内出现 \n\n。

  2. 与 customer_base 的关系
 - 无论 customer_base.contact/phone/tel 是否有值：
      - 只要 customer_contact 为空 → 不输出默认联系人行（你已选择）。
      - 若 customer_contact 非空 → 也不额外合并/去重（你已选择“不去重”），仅输出数组内容。
