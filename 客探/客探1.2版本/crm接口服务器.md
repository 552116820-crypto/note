• 这组数据基本可以排除“包太大导致 20 秒慢”。

  - 请求体只有 80 B，请求总共 370 B
  - 实际传输的响应体只有 23.28 KB
  - 浏览器解压后是 128.87 KB

  关键点是：23 KB 的网络传输量非常小。

  粗略算一下：

  - 10 Mbps 带宽下，传 23 KB 只要约 0.02s
  - 1 Mbps 带宽下，也只要约 0.18s
  - 就算只有 100 kbps，也大约 1.8s

  所以如果你看到的是 19s、21s 这种慢，问题几乎不可能是“响应大小”。这反而进一步说明：

  - 慢在服务端生成响应前，也就是 TTFB / Waiting
  - 不慢在下载响应体，也不慢在请求发送

  结合你前面的测试，结论还是一样：

  - dns/connect/tls 很快
  - 偶发慢都卡在 ttfb
  - 这说明服务端内部有慢路径，比如：
      - 某个 SQL 偶发慢
      - 某个下游接口接近 20s 超时
      - 缓存 miss 后走了重建逻辑
      - 线程池 / 连接池排队

  你现在最该看的是浏览器或网关里的两个时间：

  - Waiting (TTFB)
  - Content Download

  如果是前者接近 20s，后者只有几十毫秒，那就彻底坐实是服务端处理慢，不是报文大小问题。

  如果你愿意，我可以直接告诉你在 Chrome DevTools、Nginx、PHP/Java 里分别看哪个字段最快定位。


• 对，这次这个请求返回的数据量大概就是这个级别，但要分清两个概念：

  - 线上传输大小：23.28 KB
  - 解压后的实际 JSON 大小：128.87 KB

  也就是说，接口应该开了压缩（通常是 gzip 或 br），所以网络上真正传输的只有 23 KB 左右，浏览器解压后变成 129 KB 左右。

  这两个值都不算大：

  - 23 KB 的传输量非常小，不可能单独造成 20s
  - 129 KB 的 JSON 也不算夸张，序列化/反序列化通常也不至于拖到 20s

  所以结论还是：

  - “返回体有点大”是事实
  - 但“因为返回体大所以慢到 20 秒”基本不是事实

  真正要看的还是：

  - Waiting (TTFB) 是否接近 20s
  - Content Download 是否只有几十毫秒

  如果是这样，问题就在服务端生成响应之前，不在下载响应体。

  另外，这个大小是“这一次、这个 employee_code”对应的返回，不代表所有账号都一样。不同业务员返回的客户数量不同，响应体大小
  也会变。
