• 已把循环节点里的等待逻辑从指数退避改成固定每轮 1 秒。修改位置在 /mnt/e/KeTan_Dify/Test_SearchCustomer.yml:1653 和 /
  mnt/e/KeTan_Dify/Test_SearchCustomer/Test_SearchCustomer.yml:1653。现在这段代码直接执行 delay = 1.0，然后
  time.sleep(delay)，不会再出现 1、2、4 秒递增。

  我还校验了两个 DSL 文件都能正常被 YAML 解析。线上这个 Dify 工作流页面我这边没能直接连上浏览器自动化，所以没有直接替你
  写回网页版本；把修改后的 DSL 重新导入到这个工作流即可。如果你要，我可以继续帮你检查导入后还需不需要同步调整别的节点。
