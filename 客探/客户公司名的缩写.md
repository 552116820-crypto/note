• 结论：要加，但不建议“只靠在正文里随手加简称”。
  更稳的做法是：每条文档都带一段标准化“客户标识头”，并在检索前做一次“别名归一化”。

  你这个例子建议写成：

  【客户ID】(内部唯一ID)
  【企业全称】东莞市思纳克材料科技有限公司
  【企业简称/别名】思纳克；东莞思纳克；思纳克材料科技（按你们真实叫法）
  【统一社会信用代码】(如果有)
  【报告类型】拜访报告
  【时间】2026-02-26 22:55:44
  【正文】年后拜访了解目前客户的开工情况以及后续的产品配套...

  然后回答你“要不要每个文档块都加”：
  1. 文档不分块或块很短时

  - 在每条文档开头加一次别名，通常够用。

  2. 文档会被切成很多 chunk 时

  - 最好让每个 chunk 都带客户标识头（至少全称+简称）。
  - 否则命中的 chunk 可能没有公司名，召回会不稳。

  3. 生产级推荐（比“每块硬塞简称”更好）

  - 做一个“别名词典表”：别名 -> 标准全称/客户ID。
  - 用户提问先归一化（如“思纳克”映射为全称），再检索。
  - 这样召回率和准确率都更高，也避免简称歧义。

  补一句实操建议：
  你现在的流程里已经有 employee_code 维度，建议再加 customer_id 作为主键。后续所有检索、回写、去重都用 customer_id 做锚点，简称只作为召回辅助，不作为唯一识别条
  件。


• 可以，给你一套直接能落地在你当前流程里的做法。

  先定原则
  1. customer_id 必须是稳定且唯一的（同一客户多次拜访/更新都不变）。
  2. 优先级建议：
 
  - 用 CRM 返回的客户主键（最佳）
  - 没有就用统一社会信用代码
  - 再没有才用“员工ID+公司全称”做哈希生成（兜底）

  不要用随机 UUID，否则每次更新会变，后续无法关联。

  ———

  在你现有 Test_SearchCustomer 流程里，改这 4 处就够了

  1. 在“解析客户信息”的代码节点里新增输出

  - 新增输出字段：customer_id、customer_full_name、customer_aliases
  - 原来只输出 md 的话，改成同时返回这些字段
```python
  示例（Python）：

  import hashlib
  import re
 
  def norm_name(name: str) -> str:
      s = (name or "").strip().lower()
      s = re.sub(r"[\\s()（）·-]+", "", s)
      return s
 
  def build_customer_id(crm_customer_id: str, uscc: str, employee_code: str, company_name: str) -> str:
      if crm_customer_id:
          return str(crm_customer_id).strip()
      if uscc:
          return str(uscc).strip().upper()
      base = f"{employee_code}|{norm_name(company_name)}"
      return "CUST_" + hashlib.sha1(base.encode("utf-8")).hexdigest()[:12]

```

  2. 在文档正文头部写入 customer_id
     你现在写知识库的正文，前面加固定头：
 
  【customer_id】{{customer_id}}
  【企业全称】{{customer_full_name}}
  【企业简称/别名】{{customer_aliases}}

  然后再拼接拜访报告正文。
  这样即使 chunk 被切开，也更容易召回到正确客户。
 
  3. 在知识库 metadata 里也写 customer_id（重点）
     在 Dify 数据集里先建 metadata 字段（例如）：

  - customer_id（文本）
  - customer_full_name（文本）
  - customer_aliases（文本）
  然后你那个“metadata 更新”HTTP节点里，metadata_list 增加这几个字段赋值。
  这一步做完，后续可以按 metadata 精准过滤，不只靠语义召回。

  4. 检索前优先走 customer_id
     检索策略建议：
 
  - 先用别名映射到 customer_id（或直接从上下文拿 customer_id）
  - 按 customer_id 限定检索
  - 找不到再退化到企业全称/简称模糊检索

  ———

  一句话总结
  要加，而且要双写：正文头 + metadata，并把 customer_id 贯穿“生成 -> 入库 -> 检索 -> 回复”。
 

  如果你愿意，我可以下一步直接在你的 Test_SearchCustomer_Prod 里把这 4 处改好（包含代码节点输出、HTTP body、metadata节点、检索策略），你只需要确认 customer_id
  的来源字段名（CRM 返回里是哪一个）。
