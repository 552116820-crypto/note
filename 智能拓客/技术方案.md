1. 数据准备，客户推荐系统，三个指标不同权重，附近 + 相似 + 评分 + 多条件排序
	1. 附近客户：可以先参考crm原本的方案，目前我的想法是给每个客户加个经纬度坐标，地理索引
	2. 客户质量：评分标准，可能需要调研销售最注重哪些字段
	3. 相似客户：可以给每个客户构建空间向量，相似度检索，客户画像
2. 新增知识库PostgreSQL，三类计算，距离计算，向量相似度计算，加权得分计算
	1. MySQL 做业务主库，PostgreSQL 做推荐检索库，
	2. MySQL 5不支持构建向量的，适合“结构化过滤”