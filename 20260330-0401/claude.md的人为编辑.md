  如果你指的是 /init 生成的 CLAUDE.md，它本质上就是一个普通的 Markdown 配置/说明文件，通常就是给你后续手动维护和补充的。
  常见做法是直接编辑它，加入项目约定、运行命令、代码规范、目录说明等。

  注意两点：

  1. 再次运行 `/init`可能会改写或重建内容，改之前最好看一下 diff。
  2. 如果团队也在用它，尽量把内容写成稳定的项目规则，而不是一次性的临时备注。