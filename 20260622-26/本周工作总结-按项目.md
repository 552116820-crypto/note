本周工作总结（2026-06-22 ~ 06-26，按项目划分）

一、AI 销售系统（aisales）— 主线
本周核心，覆盖安全加固、架构解耦、代码质量与算法规则。

运维安全方案设计：针对 AI 审查结果，从 API-KEY 鉴权、生产 CORS、api_log allowlist 脱敏三方面设计方案（30%）。
JSON 日志 bug 修复：原手拼字符串易被击穿，改为 JSON 序列化（100%）。
配置优化：优化 CORS 配置、日志 allow-list 落库字段逻辑；配置 docker-compose.yml 防日志轮转打满磁盘（100%）。
架构解耦：审查发现 api 服务中 churn 与 lead 耦合，churn 启动失败会拖累 lead 接口服务，现已解耦（100%）。
代码与质量审查：代码质量审查问题不大、无需修改；工程化与安全性质量风险 AI+人工审查，结论无需改动（100%）。
接口前缀收口：/api/v1/leads 原硬编码分散多文件，现收口为单一变量（100%）。
客户得分规则：落地新的客户得分计算规则，解决分数梯度跨度大、重复、区分度低问题（100%）。
遗留：运维安全方案整体仅 30% 待推进；前缀契约泛化方法已学习，待第二个 http 面需求落地（50%）。

二、客探 dify 工作流

拜访记录接口上线：对接 CRM 工程师调整接口，支持通过 customer_name 查询公海客户拜访记录（100%）。
网关解耦：dify 从 443 端口解耦，用 edge 中立网关取代，aisales 与 dify 在 edge 下并行（100%）。
遗留：对接中发现经纬度数据有误，已提出，待 CRM 侧跟进核实。

三、智能拓客

上线准备：清空 log 表的压测与灰度流量，准备上线（100%）。
接口鉴权选型：评估鉴权方案（含验证 dify nginx auth）；因企微小程序最优方案需完整小程序后端、较复杂，最终选择静态请求头方式，防接口滥用与信息泄露（100%）。

四、CI 工程化与质量门禁

CI 工程化：实现 CI、扩大质量门禁自动化覆盖范围（80%）。
gitlab CI 验收：gitlab 上无 Runner，无法运行 CI 质量自动化检测流程（验收完成，结论为阻塞）。
测试补充：补充 churn CRM HTTP 测试与 MySQL 专属 SQL 集成测试（100%）。
阻塞：gitlab 版本低、无 Runner，CI 自动化待确认替代方案。

五、项目文档体系

同步项目文档与进度、changelog，做归一化与去重。
修复代码文件错误包名、文档结构搬迁、精炼内容、做权威文档声明（100%）。
审查文档体系质量，补充个别环境变量文档缺口、更新更正内容（50%）。
收敛提纯 claude 项目 memory，只存 git 无法保留的部分（100%）。

六、技术学习与能力建设

参加 AI 每周例会，学习产品零部件拆解 skill 搭建。
striped lock 池 / 模块级有界池（50%）；锁粒度权衡、Redis 缓存机制（20%）。
运维相关知识（50%）；前缀契约泛化方法（50%）。
opencli 工具、AI 工牌软硬件结合实现路径（50%）。
