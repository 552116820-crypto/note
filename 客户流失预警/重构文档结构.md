All tasks are complete. Here's a summary of what was done:

## 重构完成

### 1. `app/shared/` — 共享基础设施
- `models.py` — `Customer`、`Order`、`OrderItem` 共享领域模型
- `crm_repository.py` — `CrmRepository` ABC（只读 CRM 数据接口）
- `crm_memory.py` — `InMemoryCrmRepository`（内存实现 + 演示数据）
- `crm_mysql.py` — `MySQLCrmRepository`（MySQL 实现）
- `date_utils.py` — 日期工具函数
- `config.py` — 环境变量解析工具

### 2. `app/churn/` — 流失预警功能（完整迁移）
- 14 个模块，遵循 `engine → service → repository → routes` 分层
- `WarningService` 构造函数接收分离的 `CrmRepository` 和 `ChurnRepository`
- `routes.py` 使用 `APIRouter`，由 `main.py` 组合

### 3. `app/scoring/` — 客户智能打分脚手架
- 12 个模块，与 `churn/` 完全对称的分层结构
- `ScoringSettings` 带 RFM 权重配置，`ScoringRepository` ABC 已定义
- `engine.py` 标记 `NotImplementedError`，等待后续实现

### 4. 顶层入口改造
- `main.py` 通过 `include_router` 组合 churn 和 scoring 路由
- `celery_app.py` 统一 Celery 入口，注册各功能任务

### 5. 向后兼容
- `app/` 根目录下原有 12 个模块文件保留为薄 re-export 层
- 旧版 `celery -A app.tasks` 入口仍可用

### 6. 测试
- 66 项测试全部通过，目录结构与源码对应：`tests/shared/`、`tests/churn/`、`tests/scoring/`