• 可以，这组结果很好，20 并发下仍然稳定：

  成功率：500/500 = 100%
  吞吐：56.95 RPS
  平均延迟：344ms
  P95：544ms
  P99：796ms
  最大延迟：990ms
  HTTP / 业务状态：全 200

  和上一轮 10 并发相比：

  并发 10：34.03 RPS，P99 835ms
  并发 20：56.95 RPS，P99 796ms

  吞吐提升明显，延迟没有恶化，说明 concurrency=20 目前是可用档位。

  我建议正式推送参数用：

  batch_size = 50
  concurrency = 20
  timeout = 30
  max_retries = 3

  如果你的脚本/推送程序是“每个请求带 50 条 data”，这相当于理论提交能力：

  56.95 requests/s * 50 条/request ≈ 2847 条/s

  实际生产建议按保守值估算：

  1500-2500 条/s

  当前不建议继续盲目升并发。下一步更有价值的是用真实批量大小测一次，比如每个请求 50 条或 100 条，而不是重复 2 条。真实
  payload 更大时，CRM 入库压力和接口耗时可能会变。



