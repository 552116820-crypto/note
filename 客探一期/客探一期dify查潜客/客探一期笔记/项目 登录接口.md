• 可以，但不是 Dify 直接从企微拿，而是你们后端先用企业微信身份拿到 UserId，再去 CRM 映射出 employee_id，最后把 employee_id 传给 Dify。这样既安全又稳定。
  (apifox.com (https://apifox.com/apidoc/docs-site/406014/api-10061337?utm_source=openai))

  建议流程（小程序场景优先）

  1. 小程序端获取登录 code（企微小程序登录流程）。
  2. 后端调用企业微信小程序 code2session 换取用户标识（通常是 userid）和会话信息；该接口请求参数包含 access_token、js_code、grant_type。(api.eisp.em.com.cn
     (https://api.eisp.em.com.cn/Help/Api/POST-Capability-Tencent-Weixin-WeWork-JsCode2Session?utm_source=openai))
  3. 用 userid 去 CRM 做映射/查询，得到 employee_id。
  4. 调用 Dify，把 employee_id 放到 user 或 inputs.employee_id，供知识库过滤使用。

  如果是企微内 H5/网页授权（非小程序）

  - 先通过 OAuth2 拿到 code，再调用企业微信接口获取访问用户身份（返回 UserId）：(docs.wechatpy.org (https://docs.wechatpy.org/zh_CN/master/_modules/wechatpy/
    work/client/api/oauth.html?utm_source=openai))

  GET https://qyapi.weixin.qq.com/cgi-bin/user/getuserinfo?access_token=ACCESS_TOKEN&code=CODE

  (apifox.com (https://apifox.com/apidoc/docs-site/406014/api-10061337?utm_source=openai))

  关键点

  - UserId 是企业微信内部员工标识，建议在 CRM 内建立 UserId -> employee_id 映射（或直接统一）。(apifox.com (https://apifox.com/apidoc/docs-site/406014/api-
    10061337?utm_source=openai))
  - 企微 access_token / app secret 必须保存在后端，不要下发给小程序或 Dify。(apifox.com (https://apifox.com/apidoc/docs-site/406014/api-10061337?
    utm_source=openai))

  为了给你更精确的接口细节，确认两点就行：
  你们是企业微信小程序还是企微内嵌 H5？是自建应用还是第三方服务商？




• 可以这样做：后端把 employee_id 注入到 Dify 的 inputs 里，Dify Chatflow 里用这个变量做知识库过滤。下面给你一套最短路径方案（含画布设计关键点与请求模板）。

  1) 传递 employee_id 到 Dify（后端调用）

  - Dify chat-messages 支持 inputs 传自定义变量；user 是应用内唯一用户标识。(docs.dify.ai (https://docs.dify.ai/api-reference?utm_source=openai))
  - API Key 必须放在后端，避免泄露。(docs.dify.ai (https://docs.dify.ai/api-reference?utm_source=openai))

  示例请求（后端 → Dify）：

  POST {DIFY_API_BASE}/chat-messages
  Authorization: Bearer {API_KEY}
  Content-Type: application/json

  {
    "query": "查询某某公司的工商信息",
    "user": "000424",
    "inputs": {
      "employee_id": "000424"
    },
    "response_mode": "blocking"
  }

  说明：

  - 把 employee_id 放到 inputs.employee_id，供 Chatflow 变量引用。(docs.dify.ai (https://docs.dify.ai/api-reference?utm_source=openai))
  - 建议 user 也用 employee_id，便于会话隔离。(docs.dify.ai (https://docs.dify.ai/api-reference?utm_source=openai))

  ———

  2) Chatflow 设计（关键节点与变量）

  节点 A：User Input

  - 使用预设变量：userinput.query（聊天最新输入）。(docs.dify.ai (https://docs.dify.ai/en/guides/workflow/node/user-input?utm_source=openai))
  - 新增自定义变量：employee_id（Short Text）
  - 设为 Hidden，但注意：隐藏字段不能是 Required。(docs.dify.ai (https://docs.dify.ai/en/guides/workflow/node/user-input?utm_source=openai))

  节点 B：LLM（意图识别/问题改写）

  - 输入：userinput.query
  - 输出：结构化 JSON（你之前的 Schema）

  节点 C：Knowledge Retrieval

  - Query：rewrite 或 company_name
  - Knowledge Base：销售客户知识库
  - Metadata Filtering（Manual）：sales_id == employee_id
      - 只检索该销售名下 30 个客户文档。(docs.dify.ai (https://docs.dify.ai/en/use-dify/nodes/knowledge-retrieval?utm_source=openai))
  - 输出变量：result（检索结果）(docs.dify.ai (https://docs.dify.ai/en/use-dify/nodes/knowledge-retrieval?utm_source=openai))

  节点 D：LLM（信息整理输出）

  - Context：选择 result
  - 提示词：仅基于检索结果整理输出（避免编造）。(docs.dify.ai (https://docs.dify.ai/en/use-dify/nodes/knowledge-retrieval?utm_source=openai))

  ———

  3) 知识库设计（元数据字段）

  - 在每条客户文档上打元数据：sales_id
  - Chatflow 的 Knowledge Retrieval 里用 sales_id == employee_id 过滤。(docs.dify.ai (https://docs.dify.ai/en/use-dify/nodes/knowledge-retrieval?
    utm_source=openai))

  ———

  如果你要，我可以继续补：

  1. 一份完整 Chatflow 节点参数清单（可直接照抄）
  2. LLM 提示词模板（含强制 JSON 输出）
  3. 后端对接企业微信 → CRM → Dify 的完整接口串联示例



• 下面是你要的 3 部分，按“可直接照抄”的风格给出。内容不绑定具体版本界面，但字段名/变量名可以直接用。

  一、Chatflow 节点参数清单（可直接照抄）

  节点 1：User Input

  - Name: user_input
  - Inputs:
      - employee_id (Short Text)
  - Settings:
      - Hidden: true
      - Required: false
  - 说明: userinput.query 为系统默认的用户问题变量

  节点 2：LLM - Intent & Rewrite

  - Name: intent_rewrite
  - Model: 你们选的主模型
  - Inputs:
      - query = userinput.query
  - Parameters:
      - temperature = 0.1
      - max_tokens = 512（可按需）
  - Output: Structured Output (JSON Schema)
  - Output variables:
      - intent
      - company_name
      - rewrite
      - need_clarify
      - clarify_question

  节点 3：Condition - 非工商/需要澄清

  - Name: route_check
  - Branch A (need_clarify):
      - Condition: need_clarify == true
      - Next: answer_clarify
  - Branch B (非工商):
      - Condition: intent != "company_info"
      - Next: answer_other
  - Default:
      - Next: kb_retrieval

  节点 4：Answer - 澄清问题

  - Name: answer_clarify
  - Content: {{clarify_question}}

  节点 5：Answer - 非工商回退

  - Name: answer_other
  - Content: 目前仅支持查询公司工商信息，请告诉我公司全称。

  节点 6：Knowledge Retrieval

  - Name: kb_retrieval
  - Query: {{rewrite}}（为空则用 {{company_name}}，再为空用 {{userinput.query}}）
  - Knowledge Base: sales_kb
  - Search mode: Hybrid（如果界面可选）
  - Top K: 3
  - Score threshold: 0.2（可按实际调）
  - Metadata filtering:
      - sales_id == {{employee_id}}
  - Output:
      - result（检索结果列表）

  节点 7：LLM - Summarize

  - Name: summarize
  - Model: 你们选的主模型
  - Inputs:
      - query = userinput.query
      - context = kb_retrieval.result
  - Parameters:
      - temperature = 0.2
      - max_tokens = 800
  - Output:
      - final_answer

  节点 8：Answer - Final

  - Name: answer_final
  - Content: {{final_answer}}

  连线顺序

  - user_input → intent_rewrite → route_check
  - route_check → answer_clarify / answer_other / kb_retrieval
  - kb_retrieval → summarize → answer_final

  ———

  二、LLM 意图识别 Prompt 模板（强制 JSON 输出）

  你是销售助手的意图识别器。只输出 JSON，不要输出任何其它文本。

  用户问题：{{userinput.query}}

  输出 JSON（字段必须齐全）：
  {
    "intent": "company_info|other",
    "company_name": "",
    "rewrite": "",
    "need_clarify": false,
    "clarify_question": ""
  }

  规则：
  1) 识别“工商信息/企业信息/公司信息/注册信息/基本信息” => intent=company_info
  2) 抽取公司全称到 company_name
  3) 如果公司名缺失/过短/不明确 => need_clarify=true，并给出 clarifiy_question
  4) rewrite 统一格式：“查询{company_name}的工商信息”
  5) 非工商查询 => intent=other，rewrite 置空

  ———

  三、企业微信 → CRM → Dify 完整接口串联示例（后端）

  6) 小程序端

  - wx.login() 拿 code
  - 请求你们后端接口 /assistant/chat

  2) 后端流程

  - 用 code 调企业微信接口换取 userid
  - 用 userid 调 CRM 获取 employee_id
  - 调 Dify chat-messages，把 employee_id 放进 inputs

  3) 后端示例（Node/Express 伪代码）

  app.post("/assistant/chat", async (req, res) => {
    const { code, query, conversation_id } = req.body;

    // 1) 企业微信：code -> userid
    const wecomUser = await wecom.code2session(code);
    const wecomUserId = wecomUser.userid;

    // 2) CRM：userid -> employee_id
    const employee = await crm.getEmployeeIdByWecomUserId(wecomUserId);
    const employee_id = employee.employee_id;

    // 3) Dify：把 employee_id 传入 inputs
    const difyResp = await dify.chatMessages({
      query,
      user: employee_id,
      inputs: { employee_id },
      conversation_id,
      response_mode: "blocking"
    });

    res.json({
      answer: difyResp.answer,
      conversation_id: difyResp.conversation_id
    });
  });

  4) Dify 请求体模板

  POST /chat-messages
  Authorization: Bearer {DIFY_API_KEY}
  Content-Type: application/json

  {
    "query": "查询某某公司的工商信息",
    "user": "000424",
    "inputs": {
      "employee_id": "000424"
    },
    "response_mode": "blocking",
    "conversation_id": "可选"
  }

  ———

  如果你愿意，我可以再补：

  1. summarize 节点的输出模板（结构化输出 + 展示格式）
  2. 知识库文档切分/字段清洗脚本模板
  3. 小程序端调用 Dify 的“流式输出”方案（SSE）



• 可以在 Chatflow 里这样设置，让 inputs.employee_id 生效并可被后续节点使用：

  1) User Input 节点里创建 employee_id 变量

  - 打开 Chatflow 画布的 User Input 节点
  - 新增一个输入字段：
      - Type: Short Text
      - Label/Name: employee_id（建议一致、全小写）
      - Hidden: 开启（不展示给用户）
      - Required: 关闭（隐藏字段不能设为必填）(docs.dify.ai (https://docs.dify.ai/en/use-dify/nodes/user-input?utm_source=openai))

  这样就会生成一个可在后续节点使用的变量 employee_id。(docs.dify.ai (https://docs.dify.ai/en/use-dify/nodes/user-input?utm_source=openai))

  ———

  2) 后端调用时传入 inputs.employee_id
  你后端的请求体保持这样即可（Dify 支持 inputs 传入应用定义的变量）：(docs.dify.ai (https://docs.dify.ai/api-reference/chat/send-chat-message?
  utm_source=openai))

  {
    "query": "查询某某公司的工商信息",
    "user": "000424",
    "inputs": {
      "employee_id": "000424"
    },
    "response_mode": "blocking"
  }

  ———

  3) 在 Chatflow 里使用 employee_id
  示例：Knowledge Retrieval 节点过滤

  - 打开 Knowledge Retrieval 节点
  - 启用 Metadata Filtering → 选择 Manual
  - 新增条件：sales_id == employee_id
    这样检索范围就只限该销售名下的客户文档。(docs.dify.ai (https://docs.dify.ai/en/use-dify/nodes/knowledge-retrieval?utm_source=openai))

  ———

  4) 快速验证是否传入成功
  临时加一个 Answer 节点，内容填：

  employee_id = {{employee_id}}

  发送请求后如果能显示 000424，说明传参已经生效。

  ———

  如果你需要，我可以按你当前 Dify 版本给出截图式步骤（比如在哪个面板点 “Add Field / Hidden / Metadata Filter”）。
