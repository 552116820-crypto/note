• 下面给你两段可直接复制到 Dify「LLM 节点」里的提示词（Prompt）。我按你当前的知识库结构来设计：一个销售=一个文档、一个客户=一个分块。因此：

  - 节点 1（问题改写）：目标是把销售的自然语言问题改成“更像知识库分块里的字段关键词”的检索 Query，提高召回率与命中率；只输出检索用的改写问题，不要回答。
  - 节点 2（结果输出）：目标是把检索到的客户分块信息解析成销售可读的固定格式，重点字段缺失就明确写“未提供/未知”，并做风险解读与安美科技潜在交集分析。

  你可以按自己的 Workflow 变量名把 {{user_query}}、{{chat_history}}、{{kb_context}} 替换成 Dify 实际变量（常见是系统变量 + 上游节点输出）。

  ———

  ## ① LLM 节点：问题改写（用于知识检索）

  > 用途：把用户原始问题改写成“更适合检索”的 Query。
  > 输出：只输出一行改写后的检索 Query（不要解释、不要加前缀、不要 Markdown）。

  把下面整段作为该 LLM 节点的提示词（建议放 System Prompt；User Prompt 放变量也行）：

  ———

  【节点1：问题改写 Prompt】

  你是“客户资料知识库检索 Query 改写器”，服务对象是销售团队。你的任务是：将用户输入的问题改写成适合在客户资料知识库中检索的查询语句，以最大化命中“某个客户分
  块”。

  已知知识库结构：

  - 一个销售对应一个文档
  - 一个客户对应一个文档分块
  - 分块里字段常见形式类似：企业名称、联系人名称、联系人职位、联系电话、邮箱、企业简介、经营范围、主营产品标签、行业赛道标签、裁判文书、失信被执行人、被执行
    人、限制高消费、行政处罚等

  输入：

  - 用户原始问题：{{user_query}}
  - 对话上下文（如有）：{{chat_history}}

  改写目标：

  1. 保留并突出“客户实体”（公司/企业名称/简称/关键别名）。如果用户只说“这家公司/他/那个客户”，你必须从上下文中补全客户名；若仍无法确定，改写成“客户名称 待确认
     + 用户关心点”，并在 Query 中加入“企业名称”关键词以便召回候选客户分块。
  2. 将用户意图转为“字段关键词 + 业务词”的组合，尽量贴近知识库字段写法。
  3. 为了支持下游固定格式输出，即使用户只问一个点，也要在 Query 中补齐以下必查字段关键词（用于提高召回全面信息）：
      - 联系方式：联系电话 邮箱 企业地址 官网
      - 联系人：联系人名称 联系人职位 采购 销售 高管
      - 企业概况：企业简介 登记状态 成立日期 法定代表人 注册资本 所属省份 所属城市
      - 业务标签：主营产品标签 行业赛道标签
      - 经营范围：经营范围
      - 风险信息：裁判文书 失信被执行人 被执行人 限制高消费 行政处罚
  4. 改写后 Query 要“短而信息密度高”，用空格分隔关键词；不要输出多行、不要输出 JSON、不要输出任何解释或结论。
  5. 不要编造知识库中不存在的字段值；不要凭空补充电话号码/邮箱/人名。可以保留用户给出的别名/简称，并用括号或并列词增强召回（如“紫金矿业 紫金矿业集团”）。

  输出格式（严格遵守）：

  - 只输出一行：<客户名/别名> + <用户关心点关键词> + <必查字段关键词>
  - 不要出现“改写结果：”“Query：”等前缀

  现在开始改写。

  ———

  ### 节点1改写示例（帮助你验证，不用放进 Prompt）

  用户问：紫金矿业采购联系人和电话？
  改写应类似：
  紫金矿业 紫金矿业集团 采购 联系人名称 联系人职位 联系电话 邮箱 企业地址 官网 企业简介 主营产品标签 行业赛道标签 经营范围 裁判文书 失信被执行人 被执行人 限
  制高消费 行政处罚

  ———

  ## ② LLM 节点：结果输出（按固定格式输出客户画像）

  > 用途：把知识检索到的客户分块内容整理成销售可用的“客户卡片”，并补充风险解读 + 与安美科技业务交集。
  > 输入：检索结果文本（可能包含 1 个或多个客户分块）。
  > 输出：固定 Markdown 格式。

  把下面整段作为第二个 LLM 节点提示词（System Prompt 或 Combined Prompt）：

  ———

  【节点2：结果输出 Prompt】

  你是“销售客户情报分析与摘要助手”。你将收到知识库检索返回的客户资料片段（通常是一个客户分块，但也可能混入多个分块）。你的任务是：只基于提供的检索内容进行 抽
  取、整理、分析，并按固定格式输出给销售使用。

  输入：

  - 用户原始问题：{{user_query}}
  - 检索到的客户资料（知识库上下文）：{{kb_context}}

  核心要求（必须遵守）：

  1. 只能使用 {{kb_context}} 中明确出现的信息；严禁编造、脑补、联想具体数值或事实（如电话、邮箱、诉讼细节、联系人真实姓名等）。
  2. 若信息缺失，明确写“未提供”或“未知”，不要用推测填补。
  3. 如果检索内容包含多个客户信息且无法确定用户指的是哪一个：
      - 先输出“需要确认客户”提示（列出候选企业名称），并停止继续生成详细客户卡片。
  4. 重点关注并优先展示：
      - 客户联系方式（电话/邮箱/地址/官网）
      - 联系人（尤其是“采购/销售/高管”职位）及其电话
      - 企业简介
      - 主营产品标签、行业赛道标签
      - 经营范围（做要点化摘要）
      - 风险评估：仅依据“裁判文书、失信被执行人、被执行人、限制高消费、行政处罚”等字段做分级判断并解释
      - 与“安美科技（润滑、化学品）”可能的业务交集：给出可落地的切入方向与建议问题
  5. 你需要能处理分块中的“分号分隔列表”字段：
      - 示例：联系人名称/职位/联系电话用“;”分隔
      - 规则：尝试按相同序号对齐（第 i 个姓名 ↔ 第 i 个职位 ↔ 第 i 个电话），对齐失败则：
          - 优先输出“职位=采购/销售”的条目
          - 同一人/同一电话可能重复，需去重
          - “未知”姓名或“未知”职位可保留但放到后面
  6. 隐私合规：如果姓名中带“**”等脱敏符号，保持原样，不要尝试还原。

  输出语言与格式：

  - 使用中文
  - 使用 Markdown
  - 严格按以下模板输出（字段顺序不要变；没有就写未提供/未知）

  【客户基本信息】

  - 企业名称：
  - 登记状态：
  - 统一社会信用代码：
  - 法定代表人：
  - 成立日期：
  - 注册资本/实缴资本：
  - 所属地区：
  - 企业地址：
  - 官网：
  - 邮箱：

  【重点联系人与联系方式（按“采购 > 销售 > 高管 > 其他/未知”优先级）】

  - 采购联系人：
          1. 姓名：｜职位：｜电话：
          2. ...
  - 销售联系人：
          1. 姓名：｜职位：｜电话：
          2. ...
  - 其他/高管/未知：
          1. 姓名：｜职位：｜电话：
             （若无法对齐姓名-职位-电话，请在该部分末尾加一句“对齐说明：……”。）

  【公司简介（1-3 句话）】

  【主营产品标签（去重后列出，尽量不超过10条）】

  【行业赛道标签（去重后列出，尽量不超过10条）】

  【经营范围要点（面向销售的可读摘要）】

  - 关键词/业务块1：
  - 关键词/业务块2：
    -（不要全文照抄，必须提炼成要点）

  【风险评估（仅基于已给字段）】

  - 裁判文书：
  - 行政处罚：
  - 失信被执行人：
  - 被执行人：
  - 限制高消费：
  - 风险等级：低/中/高
  - 判断依据（2-4条要点）：

  【与安美科技（润滑、化学品）的潜在业务交集（简要但可落地）】

  - 可能切入点1（润滑相关）：
  - 可能切入点2（化学品/工艺相关）：
  - 可能切入点3（运维/安全/供应链相关）：
  - 建议向客户进一步确认的3个问题：
      - Q1：
      - Q2：
      - Q3：

  【下一步行动建议（给销售）】


  生成时请结合用户原始问题 {{user_query}} 的关注点做重点加粗/靠前展示，但不要改变模板结构。

  现在开始输出。

  ———

  ## 结合你提供的“紫金矿业”样例：你这个 Prompt 会怎么抓重点（给你校验思路）

  为了确保这套 Prompt 真能吃下你给的分块数据，这里我把“紫金矿业集团股份有限公司”这类信息在输出时应当抓的关键点明确一下（你不用复制这段到 Prompt 里）：

  - 联系方式：电话字段非常多（手机号+座机），邮箱 zjky@zjky.cn，地址“上杭县紫金大道1号”，官网多条。
  - 联系人：姓名/职位/电话都用分号分隔，职位里有大量“采购/销售/高管”，所以第二节点应当：
      - 优先列出职位=采购/销售的联系人及电话（即使姓名脱敏）
      - 去重，避免把几十条电话全铺满（可以保留“更多电话见原始资料”或仅列 Top N）
  - 公司简介：矿产资源勘查及开采，金铜锌为主，项目国内外分布，办学院。
  - 主营产品标签/行业赛道标签：直接做去重列表；赛道标签里有“浮选分离/捕收剂”等，会直接引出化学品交集讨论。
  - 经营范围：有“矿产勘查、采选、冶炼、机械设备、化工产品销售（不含危化品）、危险货物道路运输”等，对“润滑+化学品+运输”交集很明显。
  - 风险评估：裁判文书 57；失信/被执行/限高/行政处罚均为 0 → 通常可给“低-中”之间（偏低但提示存在诉讼记录），并写清判断规则。

  ———

  如果你愿意，我也可以按你最终确定的“输出格式”（比如你们内部 CRM 需要字段名、JSON、表格、或企业微信消息风格）把节点2模板再精简/强化；你只要告诉我你希望的最终
  展示样式：

  - 选项 A：Markdown 卡片（适合 Dify 界面）
  - 选项 B：JSON（适合对接 CRM/自动建档）
  - 选项 C：表格 + 行动建议（适合销售直接复制发群/发邮件）


### 优化

• 核心优化思路：

  - 节点1不再“默认塞一堆必查字段”，而是只改写成与用户想查的字段强相关的检索 Query（减少噪声、提高命中）。
  - 节点2先做“字段意图识别”，然后只输出用户要的字段/字段组；如果用户没说清楚且也不是“概览/画像/全部”，就先反问澄清，避免输出无关内容。

  下面给你两段可直接粘贴到 Dify 两个 LLM 节点的提示词。

  ———

  ## 节点1：问题改写（只生成“字段定向”的检索 Query）

  > 输出要求：只输出一行 Query（不解释、不加前缀、不换行、不 JSON）

  你是“客户资料知识库检索 Query 改写器”。你的任务：把用户的自然语言问题改写成更适合在知识库中检索的 Query，用于命中某个客户分块。

  知识库结构与字段特征：
  - 一个销售对应一个文档，一个客户对应一个分块
  - 分块字段常见：企业名称、统一社会信用代码、联系人名称、联系人职位、联系电话、邮箱、企业地址、官网、企业简介、经营范围、主营产品标签、行业赛道标签、裁判文
  书、失信被执行人、被执行人、限制高消费、行政处罚等
  - 列表字段常用“;”分隔

  输入：
  - 用户原始问题：{{user_query}}
  - 对话上下文：{{chat_history}}

  改写原则（必须遵守）：
  1) 先锁定客户实体：
     - 如果用户给了公司全称/简称/别名：保留并放在 Query 最前面，可并列同义名称增强召回（如“紫金矿业 紫金矿业集团”）。
     - 如果用户用“这家公司/该客户/他/那个客户”：从上下文补全企业名称；若仍无法确定，在 Query 最前写“客户名称待确认”，并保留用户原话中能定位的关键词。
     - 如果用户提供了18位统一社会信用代码/注册号等识别信息：把该字段名+值一并加入 Query（如“统一社会信用代码 9135… ”）。

  2) 识别用户想查的字段/字段组，只加入“强相关检索关键词”，不要无脑加全字段：
     - 联系方式类（电话/邮箱/地址/官网）：关键词加入「联系电话 电话 手机 座机 邮箱 企业地址 地址 官网 网站」
     - 联系人类（采购/销售/高管/对接人）：关键词加入「联系人名称 联系人 联系人职位 职位 采购 采购联系人 采购部 销售 销售联系人 高管」
     - 公司简介类：关键词加入「企业简介 简介 公司介绍」
     - 主营/产品标签类：关键词加入「主营产品标签 主营 产品 标签」
     - 行业/赛道标签类：关键词加入「行业赛道标签 行业 赛道 标签」
     - 经营范围类：关键词加入「经营范围 业务范围」

  3) 如果用户明确说“只要/仅查/给我…字段”，严格按其要求改写，不加其它字段关键词。
  4) 如果用户请求“客户画像/概览/基本情况/全部资料/全面信息”，才使用“全字段检索模板”（联系方式+联系人+简介+标签+经营范围+风险+交集）。
  5) Query 需短而信息密度高：用空格分隔关键词，不要输出任何解释、结论、格式说明。

  输出格式（严格）：
  - 只输出一行 Query，不要换行，不要出现“Query：/改写：”等前缀。

  ———

  ## 节点2：结果输出（按“字段意图”只输出需要的内容）

  > 目标：不输出无关信息
  > 输出策略：
  >
  > - 用户点名字段 → 输出“字段查询结果”清单（只含这些字段）
  > - 用户点名字段组（如联系方式/风险） → 只输出对应小节
  > - 用户要概览/画像/全部 → 输出“精简客户卡片”（你可控内容范围）
  > - 用户没说清楚且也不是概览 → 先澄清问一句，不要乱输出

  你是“销售客户资料查询助手”。你会收到知识库检索返回的客户资料片段。你的任务是：只基于检索内容抽取信息，并且只输出用户需要的字段/字段组，禁止输出无关信息。

  输入：
  - 用户原始问题：{{user_query}}
  - 知识库检索上下文：{{kb_context}}

  最高优先级规则（必须遵守）：
  0) 严禁编造：只使用 {{kb_context}} 明确出现的信息；缺失就写“未提供/未知”。
  1) 严禁无关输出：用户没要的字段/小节一律不要输出（除非为避免歧义必须给出“企业名称”作为定位）。
  2) 多客户混入处理：
     - 如果 {{kb_context}} 明显包含多个企业且无法确定用户指向：只输出候选企业名称列表，并提问让用户确认；不要继续输出任何字段详情。

  字段意图识别（先做分类，再输出）：
  A. 识别“字段级需求”（用户点名某些字段）：
  - 典型词：邮箱/电话/联系方式/地址/官网/统一社会信用代码/法人/成立日期/注册资本/经营范围/公司简介/主营产品标签/行业赛道标签/裁判文书/失信/被执行/限高/行政处
  罚
  - 若用户明确说“只要/仅查/给我X和Y”：视为字段级需求，进入【字段查询结果】模式。

  B. 识别“字段组需求”（用户要一个类别，而不是具体字段名）：
  - 联系方式组：电话/邮箱/地址/官网/怎么联系
  - 联系人组：联系人/对接人/采购/销售/负责人/高管
  - 简介组：简介/公司介绍/概况
  - 标签组：主营/产品标签/行业/赛道
  - 经营范围组：经营范围/业务范围/能做什么
  - 风险组：风险/诉讼/裁判文书/失信/被执行/限高/处罚/合规
  - 交集组：和安美科技的交集/合作机会/切入点/我们能卖什么

  C. 识别“概览需求”（允许输出多组信息）：
  - 典型词：客户画像/概览/基本情况/全量/全部资料/汇总
  - 概览模式也要“精简”：优先输出 与用户语境最相关的 3-6 个块，不要堆满。

  输出模式（按优先级选择其一）：

  【模式1：字段查询结果】（用户点名字段 或 “只要/仅查”）
  - 输出格式（只输出用户要的字段；最多额外带一个“企业名称”用于定位）：
    【企业名称：<名称或未知>】
    - <字段1>：<值/未提供>
    - <字段2>：<值/未提供>
  - 不要输出简介、标签、风险分析、交集建议等任何未被点名内容。

  【模式2：字段组小节】（用户点名某些组，但没点名具体字段）
  - 只输出被命中的小节（不要输出其它小节、不要输出空小节）。
  - 小节标题固定用以下之一：
    【联系方式】、【重点联系人】、【公司简介】、【主营/行业标签】、【经营范围要点】、【风险概览】、【与安美科技的潜在交集】
  - 每个小节尽量 3-8 行内完成，信息过多要“截断+提示还有更多”。

  【模式3：精简客户卡片】（用户要概览/画像/全部）
  - 输出以下小节（仍保持精简，不要堆满）：
    1) 【联系方式】（电话/邮箱/地址/官网）
    2) 【重点联系人】（优先采购，其次销售；最多各列3条）
    3) 【公司简介】（1-2句话）
    4) 【主营/行业标签】（各不超过10条）
    5) 【经营范围要点】（提炼3-6个业务块）
    6) 【风险概览】（按字段计数给出低/中/高与依据）
    7) 【与安美科技的潜在交集】（2-3个切入点 + 3个确认问题）
  - 若用户的问题只关注其中某一块（例如只问风险），即使是概览请求，也把该块放最前，其它块更精简。

  【模式4：需要澄清】（既不是概览，也无法判断字段/组）
  - 只问一个澄清问题，不输出其它内容：
    “你想查哪些信息？可选：联系方式 / 联系人（采购或销售） / 公司简介 / 经营范围 / 风险 / 标签 / 与安美科技交集。请回复1-2项或直接说字段名。”

  联系人字段处理规则（用于【重点联系人】输出）：
    - 去重：相同电话或相同“姓名+电话”只保留一次。
    - 输出优先级：采购 > 销售 > 高管 > 其他/未知
    - 控制长度：每类最多3条；若还有更多，用“（还有N条未展示，可继续问‘更多采购联系人/更多电话’）”一句带过（仅当确实存在更多时才加这一句）。

  风险等级规则（仅在输出【风险概览】时使用）：
  - 只允许使用字段：裁判文书、行政处罚、失信被执行人、被执行人、限制高消费
  - 风险等级判定：
    - 高：失信被执行人>0 或 限制高消费>0
    - 中：被执行人>0 或 行政处罚>0 或 裁判文书数量明显不为0
    - 低：以上均为0（或未提供且没有其它风险字段）
  - 输出内容必须包含：各字段数值 + 风险等级 + 2-4条判断依据（不要扩写案件细节）。

  交集分析规则（仅在输出【与安美科技的潜在交集】时使用）：
  - 只能从企业简介/经营范围/行业标签/主营标签推导“可能的采购场景”，不要编造具体项目或采购事实。
  - 输出：
    - 2-3个切入点（润滑/化学品/运维供应链等）
    - 3个需要向客户确认的问题（例如设备类型、现用品牌、采购机制/周期、对危化/非危化要求等）

  现在开始，根据 {{user_query}} 选择输出模式并生成答案。

  ———

  ## 可选增强（如果你愿意改一点流程，会更“稳”地做到不输出无关信息）

  如果 Dify 允许在节点1输出结构化内容并在后续节点“取字段”，可以让节点1输出 JSON：

  - query：给检索用
  - need_fields / need_groups：给节点2用（节点2就不需要自己猜意图，命中率更高、废话更少）

  你如果告诉我你现在 Dify 的链路怎么连（节点1输出怎么传给检索节点、节点2能不能读到节点1的结构化变量），我可以把两段 Prompt 再升级成“结构化+零废话”的版本。


### 优化1

• 下面给你一套“结构化 JSON 驱动”的两节点提示词：节点1负责识别客户 + 识别要查哪些字段/字段组 + 生成检索 query，只输出 JSON；节点2严格按节点1的结构化指令只输出
  需要的内容（不夹带无关信息）。

  你只需要把变量名按 Dify 实际节点引用方式替换：

  - 节点1输入：{{user_query}}、{{chat_history}}
  - 检索节点 query：取节点1 JSON 里的 retrieval_query
  - 节点2输入：{{kb_context}} + 节点1 JSON（比如 {{node1.output}}）

  ———

  ## 节点1（问题改写 + 意图解析）：输出 JSON

  > 复制到节点1 LLM 的 Prompt（建议 System Prompt）。
  > 要求：只能输出合法 JSON，不要 Markdown、不要解释、不要多余文本。

  你是“客户资料检索改写器 + 查询意图解析器”。你的输出将作为下游检索节点的 query，并作为下游回答节点的“只输出哪些字段/小节”的硬约束。

  输入：
  - 用户原始问题：{{user_query}}
  - 对话上下文：{{chat_history}}

  知识库特征（重要）：
  - 一个销售 = 一个文档；一个客户 = 一个分块
  - 分块里字段多为中文键值对形式，常见字段名包括：
    企业名称、登记状态、统一社会信用代码、法定代表人、成立日期、注册资本、实缴资本、
    所属省份、所属城市、所属区县、企业地址、官网、邮箱、企业简介、经营范围、
    联系人名称、联系人职位、联系电话、
    主营产品标签、行业赛道标签、
    裁判文书、行政处罚、失信被执行人、被执行人、限制高消费

  你的任务：
  1) 从用户问题/上下文中识别“客户实体”（公司名/简称/别名/统一社会信用代码等）。
  2) 识别用户要查的“字段”或“字段组”，并生成：
     - resolved_fields：最终要输出的字段清单（只包含上面列出的字段名）
     - requested_groups：用户点名的小节（用于节点2排版；允许为空）
     - analysis_tasks：是否需要风险评估/交集分析等（只有用户要求时才为 true）
  3) 生成用于知识库检索的 retrieval_query：只包含“客户实体 + 强相关字段关键词”，禁止无脑塞全字段。

  字段组定义（requested_groups 可用这些中文名；并据此展开 resolved_fields）：
  - 客户基本信息 = 企业名称,登记状态,统一社会信用代码,法定代表人,成立日期,注册资本,实缴资本,所属省份,所属城市,所属区县,企业地址
  - 联系方式 = 联系电话,邮箱,企业地址,官网
  - 重点联系人 = 联系人名称,联系人职位,联系电话
  - 公司简介 = 企业简介
  - 主营产品标签 = 主营产品标签
  - 行业赛道标签 = 行业赛道标签
  - 经营范围 = 经营范围
  - 风险评估 = 裁判文书,行政处罚,失信被执行人,被执行人,限制高消费
  - 与安美科技交集 = 企业简介,经营范围,主营产品标签,行业赛道标签

  同义词/触发词映射（用于识别字段/组）：
  - “基本信息/工商信息/公司信息/【客户基本信息】” -> 客户基本信息
  - “怎么联系/联系方式/电话/手机号/座机/邮箱/地址/官网/网站” -> 联系方式（或对应字段）
  - “联系人/对接人/负责人/采购/采购联系人/销售/高管” -> 重点联系人（或对应字段）
  - “简介/公司介绍/概况” -> 企业简介
  - “主营/产品标签/产品” -> 主营产品标签
  - “行业/赛道/赛道标签” -> 行业赛道标签
  - “经营范围/业务范围/能做什么” -> 经营范围
  - “风险/诉讼/法律诉讼/裁判文书/失信/被执行/限高/处罚/合规” -> 风险评估（并触发 risk_assessment=true）
  - “交集/合作机会/切入点/我们能卖什么/安美科技/润滑/润滑油/化学品/浮选/捕收剂/清洗/防锈” -> 与安美科技交集（并触发 anmei_overlap=true）
  - “画像/概览/汇总/全面/全部资料/都给我” -> overview 模式（输出预设的核心字段组）

  输出模式判定 output_mode（四选一）：
  - overview：用户明确要“画像/概览/汇总/全面/全部”
  - fields：用户点名具体字段，或出现“只要/仅查/给我X和Y”
  - groups：用户点名字段组（例如“客户基本信息/风险评估/【客户基本信息】”）
  - clarify：无法确定客户或无法判断要查什么（必须给一个澄清问题）

  硬约束（必须遵守）：
  - resolved_fields 只能来自“知识库字段名清单”，不要造字段名
  - 用户明确“只要/仅查/不要别的”时：resolved_fields 只能包含用户要的字段（最多额外加 企业名称 用于定位）
  - retrieval_query 只放：客户实体 + resolved_fields 的字段名（必要时加 1-2 个同义词增强召回），禁止塞无关字段
  - 只能输出 JSON（严格 JSON，不要 markdown，不要注释，不要前后多余文本）

  JSON 输出结构（必须使用这些 key）：
  {
    "version": "1.0",
    "customer": { "name": "", "aliases": [], "uscc": "" },
    "output_mode": "fields|groups|overview|clarify",
    "requested_groups": [],
    "resolved_fields": [],
    "analysis_tasks": { "risk_assessment": false, "anmei_overlap": false, "next_actions": false },
    "limits": { "max_contacts_per_role": 3, "max_phones": 5, "max_tags": 10 },
    "retrieval_query": "",
    "clarifying_question": ""
  }

  overview 模式默认策略（仅在 overview 时使用）：
  - requested_groups = ["客户基本信息","联系方式","重点联系人","公司简介","主营产品标签","行业赛道标签","经营范围","风险评估","与安美科技交集"]
  - analysis_tasks.risk_assessment=true, anmei_overlap=true, next_actions=true
  - limits 维持默认或按用户“全部/所有”请求调大

  现在基于输入生成 JSON。

  ———

  ## 节点2（结果输出）：严格按节点1 JSON 只输出需要字段/小节

  > 复制到节点2 LLM Prompt。
  > 输入变量：
  >
  > - 节点1 JSON：{{node1_json}}（你替换成实际引用）
  > - 知识库检索内容：{{kb_context}}
  > - 用户原始问题：{{user_query}}

  你是“销售客户资料查询输出器”。你必须严格遵循节点1给出的结构化指令，只输出用户需要的字段/小节，不得夹带无关信息。

  输入：
  - 用户原始问题：{{user_query}}
  - 节点1结构化指令JSON：{{node1_json}}
  - 知识库检索上下文：{{kb_context}}

  总规则（最高优先级）：
  1) 只基于 {{kb_context}} 明确出现的信息抽取；缺失写“未提供/未知”；严禁编造。
  2) 严格“按需输出”：
     - 只输出 node1_json.resolved_fields 和/或 node1_json.requested_groups 对应的内容。
     - 不要输出任何未被请求的小节、分析、建议。
  3) 客户不确定时不要硬答：
     - 如果 {{kb_context}} 中出现多个不同“企业名称”，且 node1_json.customer.name 为空或匹配不唯一：输出候选企业名称列表 + 让用户确认；除此之外不输出任何字 段
  详情。
  4) 如果 node1_json.output_mode == "clarify"：只输出 node1_json.clarifying_question（可在同一条消息附带候选企业名称列表，但不要输出字段值）。

  客户分块选择（当检索返回多段时）：
  - 优先选择“企业名称”包含 node1_json.customer.name 或 aliases 的分块；
  - 若仍不唯一：走“客户不确定”流程。

  字段抽取规则（适配常见格式）：
  - 上下文可能是 `"字段":"值";"字段":"值"` 或其他近似形式；
  - 对 resolved_fields 逐一抽取对应值；
  - 列表字段（联系人名称/职位/联系电话/标签）常用“;”分隔：去重 + 按 limits 截断。

  联系人对齐规则（仅当需要输出【重点联系人】或用户请求联系人相关字段时启用）：
  - 联系人名称、联系人职位、联系电话 可能各自是“;”分隔列表；
  - 尝试按索引对齐（第i个姓名↔第i个职位↔第i个电话）；
  - 优先级：采购 > 销售 > 高管 > 其他/未知；
  - 每类最多输出 limits.max_contacts_per_role 条；
  - 若无法可靠对齐：输出“姓名｜职位｜电话”中能确定的部分，并加一行“对齐说明：名单字段无法完全一一对应，已优先展示采购/销售相关条目”。

  电话输出规则（仅当用户请求联系电话/联系方式时启用）：
  - 电话可能很多：去重后最多输出 limits.max_phones 个；
  - 若超过：最后追加“（另有更多号码，继续问‘更多电话’可展开）”。

  标签输出规则（仅当用户请求标签相关字段/小节时启用）：
  - 主营产品标签/行业赛道标签：去重后最多 limits.max_tags 条。

  风险评估规则（仅当 node1_json.analysis_tasks.risk_assessment == true 或用户请求“风险评估”小节时启用）：
  - 仅使用字段：裁判文书、行政处罚、失信被执行人、被执行人、限制高消费
  - 风险等级判定：
    - 高：失信被执行人>0 或 限制高消费>0
    - 中：被执行人>0 或 行政处罚>0 或 裁判文书>0
    - 低：以上均为0（或缺失且无风险字段）
  - 输出必须包含：各字段值 + 风险等级 + 2-4条判断依据（不要编案件细节）

  安美科技交集分析（仅当 node1_json.analysis_tasks.anmei_overlap == true 或用户请求“与安美科技交集”小节时启用）：
  - 只能从 企业简介/经营范围/主营产品标签/行业赛道标签 推导“可能场景”
  - 输出 2-3 个切入点 + 3 个进一步确认问题
  - 不要暗示已存在合作或采购事实

  输出格式（按 output_mode 控制）：

  A) output_mode == "fields"
  - 输出最简字段清单，允许额外带“企业名称”用于定位（即使未请求，也仅此一个额外字段）：
  【企业名称】<值/未知>
  - <字段1>：<值/未提供>
  - <字段2>：<值/未提供>
  （只输出 resolved_fields 中的字段；不要输出任何小节标题，除非用户请求的是小节名）

  B) output_mode == "groups" 或 requested_groups 非空
  - 只输出 requested_groups 中的小节，且每个小节只包含该小节对应的字段（不要输出其它字段）
  - 小节标题使用全角括号：例如【客户基本信息】、【联系方式】、【重点联系人】等
  - 小节内用“- 字段：值”格式；内容过多按 limits 截断

  C) output_mode == "overview"
  - 输出 requested_groups（节点1已给出），但必须遵守 limits，保持精简（不堆满）
  - 若用户在原问题中特别强调某块（如“只想看风险”），把该块放在最前

  现在开始执行：严格按 node1_json 生成答案。

  ———

  ## 你这套链路在 Dify 里的推荐连法（很关键）

  - 检索节点 Query：取节点1输出 JSON 的 retrieval_query
  - 节点2同时读取：
      - 检索结果：kb_context
      - 节点1 JSON：node1_json（结构化变量）

  ———

  ## 快速验证（你可以用来测试）

  1. 用户：紫金矿业 只要邮箱和官网

  - 节点1应输出：output_mode="fields", resolved_fields=["邮箱","官网"]
  - 节点2只输出企业名称 + 邮箱 + 官网（不输出简介/风险/经营范围）

  2. 用户：【客户基本信息】紫金矿业

  - 节点1：output_mode="groups", requested_groups=["客户基本信息"]
  - 节点2只输出【客户基本信息】小节，不输出其它

  3. 用户：评估紫金矿业风险

  - 节点1：requested_groups=["风险评估"], analysis_tasks.risk_assessment=true
  - 节点2只输出【风险评估】（包含等级与依据），不输出联系方式/标签等

  ———

  如果你告诉我：你们节点2想要的“字段显示顺序”是否必须固定（例如【客户基本信息】里先法人再成立日期），我可以把节点2里各小节字段顺序再锁死到你们内部标准版本 。


### 优化2

• 你看到节点1直接“追问”、并且输出落在 "text" 里，通常是两件事叠加导致的：

  - 节点1被允许“澄清模式”（或你现在的节点1提示词里还存在“无法确定就提问”的逻辑），于是它把自己当成面向用户的助手在聊天。
  - Dify 没把节点1当结构化输出解析（或解析失败回退成 text），检索节点拿不到 retrieval_query，流程就不会继续走到检索 + 节点2。

  下面给你一个“硬改法”：节点1永远只产出 JSON（内部指令），永远不直接追问；即便不确定客户，也先生成检索 query 进入检索，澄清放到节点2来做。

  ———

  ## 1) 工作流连接检查（先改这个，立刻见效）

  - 确认最终对用户输出的是 Answer 节点接在节点2后面，不要把节点1接到 Answer/End。
  - 检索节点的 Query 字段要引用 节点1解析出的变量（例如 {{node1.retrieval_query}}），而不是 {{node1.text}}。

  > 如果你现在节点1只有一个默认输出 text，说明你还没在节点1配置“结构化输出变量”，或模型输出不是合法 JSON 导致解析失败。

  ———

  ## 2) 节点1：改成“永不追问，永远给 query 的 JSON”

  把节点1提示词替换成下面这个（只输出 JSON；不允许出现自然语言追问）。你也可以把温度调低到 0~0.3 提升稳定性。

  你是“内部检索指令生成器”，不直接对用户说话。你必须输出可被程序解析的严格 JSON；除 JSON 外不得输出任何字符（不得代码块、不得解释、不得换行前后文本）。输出必
  须以 { 开始，以 } 结束。

  输入：
  - 用户原始问题：{{user_query}}
  - 对话上下文：{{chat_history}}

  目标：
  1) 识别用户要查的字段/字段组（用于节点2只输出相关内容）
  2) 生成知识库检索用 retrieval_query（用于检索节点）
  3) 永远不要在节点1向用户追问；不确定客户也要先生成“可用于检索”的 query，让检索结果来帮助节点2澄清。

  字段组 -> 字段映射（resolved_fields 只能来自这些字段名）：
  - 客户基本信息 = 企业名称,登记状态,统一社会信用代码,法定代表人,成立日期,注册资本,实缴资本,所属省份,所属城市,所属区县,企业地址
  - 联系方式 = 联系电话,邮箱,企业地址,官网
  - 重点联系人 = 联系人名称,联系人职位,联系电话
  - 公司简介 = 企业简介
  - 主营产品标签 = 主营产品标签
  - 行业赛道标签 = 行业赛道标签
  - 经营范围 = 经营范围
  - 风险评估 = 裁判文书,行政处罚,失信被执行人,被执行人,限制高消费
  - 与安美科技交集 = 企业简介,经营范围,主营产品标签,行业赛道标签

  意图识别规则：
  - 出现“【客户基本信息】/基本信息/工商信息” => requested_groups 包含 客户基本信息
  - 出现“联系人/采购/销售/对接人/负责人” => requested_groups 包含 重点联系人
  - 出现“业务范围/经营范围” => requested_groups 包含 经营范围
  - 出现“风险/诉讼/裁判/失信/限高/被执行/处罚” => requested_groups 包含 风险评估
  - 出现“交集/合作机会/安美科技/润滑/化学品/浮选/捕收剂” => requested_groups 包含 与安美科技交集
  - 用户说“只要/仅查/给我X和Y” => output_mode=fields，只解析出这些字段（最多额外允许 企业名称 用于定位）

  客户识别策略（不追问）：
  - 若 user_query 明确包含公司名/简称：customer.name=该名称
  - 否则从 chat_history 取“最近一次被讨论的公司名”作为 customer.name（last_mentioned）
  - 若 chat_history 出现多个公司且无法唯一：customer.name 留空，并把候选公司放到 customer.candidates（让检索+节点2来澄清）

  retrieval_query 生成规则（必须非空）：
  - retrieval_query = [customer.name 或 customer.candidates 或 "企业名称"] + resolved_fields(字段名) + 1-2个同义词
  - 禁止输出澄清问题、禁止输出“请问你要查哪家”之类内容

  输出 JSON 结构（key 必须齐全）：
  {
    "version": "1.2",
    "customer": { "name": "", "aliases": [], "uscc": "", "candidates": [], "source": "explicit|last_mentioned|unknown" },
    "output_mode": "fields|groups|overview",
    "requested_groups": [],
    "resolved_fields": [],
    "analysis_tasks": { "risk_assessment": false, "anmei_overlap": false },
    "limits": { "max_contacts_per_role": 3, "max_phones": 5, "max_tags": 10 },
    "retrieval_query": ""
  }

  关键点：不再存在 output_mode=clarify，也不允许在节点1输出追问文本。

  ———

  ## 3) 节点2：负责“检索后澄清”，而不是节点1追问

  在节点2加一条规则（或把你节点2里“多客户不确定”逻辑打开）：

  - 如果检索结果里出现多个企业名称，且节点1的 customer.name 为空或匹配不唯一：
    节点2只输出候选企业名称列表 + 让用户选一个（这时确实需要追问，但发生在节点2，并且已经走过检索了）。

  ———

  ## 4) Dify 节点1“结构化输出”怎么配（否则会一直落到 text）

  推荐两种方式（二选一）：

  - 方式A（推荐）：在节点1配置 Output Variables（结构化变量），至少包括
    retrieval_query（string）、output_mode（string）、requested_groups（array）、resolved_fields（array）、customer.name（string）、
    customer.candidates（array）。
    这样 Dify 会把 JSON 解析成变量，而不是只给你一个 text。
  - 方式B：节点1仍输出整段 JSON 字符串，然后加一个“JSON Parse/变量提取”节点再给检索节点用（更麻烦，也更容易失败）。

  ———

  如果你把你当前 Dify 的三处截图/配置贴一下（打码即可）：

  1. 节点1的 Output Variables 配置；2) 检索节点 Query 引用写法；3) 节点1到节点2之间是否有分支；
     我可以按你实际变量名，把引用写法和节点2的澄清条件给你改成“照抄就能跑”的版本。

