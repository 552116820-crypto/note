如果你愿意，我可以直接帮你把这版改成“同音字更稳”的方案（加拼音/别名入库 + 查询纠错 + 结果二次校验）

• 目标
  把“语音同音字误识别”从“偶尔能命中”提升到“多数场景可命中且不乱答”。

  现状问题
  1. 你现在是混合检索（关键词+向量）但没有阈值，容易“有结果但不准”。Test_SearchCustomer.yml:485 Test_SearchCustomer.yml:489 Test_SearchCustomer.yml:493
  2. 检索后只判断“结果是否为空”，没有判断“是不是同一家企业”。Test_SearchCustomer.yml:1360     
  3. 入库文本主要是标准企业名，没有专门的同音容错字段。Test_SearchCustomer.yml:918

  我给你的方案（推荐完整版）

  4. 查询容错层（新增代码节点，放在“知识检索”前）

  - 做法：把 sys.query 生成多种变体再检索。
  - 变体包括：原句、去公司后缀、去城市前缀、去括号、去标点、常见同音混淆替换（可维护小词表）。
  - 价值：ASR 把“思纳克”识别成“斯纳克”时，仍有机会命中。

  2. 检索参数层（改当前知识检索节点）
  - 做法：top_k 从 3 提到 8；vector_weight 提高到 0.7，keyword_weight 降到 0.3；加 score_threshold（例如 0.42 起步）。
  - 价值：对错字/同音字更鲁棒，同时减少明显无关召回。
  - 位置：Test_SearchCustomer.yml:486 Test_SearchCustomer.yml:489

  3. 命中校验层（新增检索后代码节点 + 分支）

  - 做法：从 1769758736207.result 解析候选企业名，和用户 query 做相似度打分（标准化 + 编辑距离）。
  - 分流规则：高分唯一命中 -> 交给 LLM；多家接近 -> 只返回候选名单让用户选；都不够像 -> 固定“未检索到”。
  - 价值：避免“检索到了别家企业还继续回答”。
 
  4. 入库增强层（改入库渲染代码）

  - 做法：在每个企业块增加“别名行”，自动写入：
      - 原名
      - 去后缀名（去“有限公司/股份有限公司”等）
      - 去地域前缀名（如“东莞市”）
      - 去括号名
  - 价值：不依赖外部拼音库，也能覆盖大量 ASR 常见误差。
  - 位置：企业块生成代码在 Test_SearchCustomer.yml:912 附近。


  4. 完整版（约 2-3 小时，推荐）
 
  - 做第 1/2/3/4/5 全套。
  - 优点：同音字、错字、简称都能明显改善。
  - 缺点：节点和代码会多一点。
 

  我建议直接做“完整版”。如果你同意，我下一步就开始改 Test_SearchCustomer.yml 和验收脚本，并给你一份可直接导入/发布的结果。



• 已把这版 Test_SearchCustomer 工作流改成你要的“三层稳态方案”：拼音/别名入库 + 查询纠错 + 结果二次校验，并且保留了你原来的建库/更新链路。

  改动落点

  - 入库增强（拼音/别名入库）
      - 我重写了入库代码节点 1770194090406，现在每个企业块都会额外写入 【检索增强】，包含：
      - 检索别名（来自 former_name/historyNames/... + 简称化）
      - 检索拼音（优先取上游字段如 *_pinyin，并兼容 english_name）
      - 检索关键词（企业名+别名+去后缀关键词）
      - 位置：Test_SearchCustomer.yml:852 / Test_SearchCustomer.yml:870
  - 查询纠错（语音同音字兜底）
      - 新增代码节点 1773001001001（Query Normalize），输入 sys.query，输出：
      - retrieval_query（原句 + 归一化 + 同音纠错候选 + 后缀变体）
      - normalized_query
      - keyword_hint
      - correction_notes
      - 节点定义位置：Test_SearchCustomer.yml:1418 / Test_SearchCustomer.yml:1445
  - 检索输入切换
      - 知识检索 的 query_variable_selector 已从 sys.query 改为 1773001001001.retrieval_query
      - 位置：Test_SearchCustomer.yml:502
  - 结果二次校验（防止错召回直接回答）
      - 新增代码节点 1773001002001（Result Validate），对检索结果做二次打分校验：
      - 从召回块提取 企业名称/检索别名/检索拼音/检索关键词
      - 与纠错后的 keyword_hint/normalized_query 做匹配置信度判断
      - 输出 is_confident 等变量
      - 节点定义位置：Test_SearchCustomer.yml:1458 / Test_SearchCustomer.yml:1503
  - 分支判断改造
      - 原来“只要检索非空就进 LLM”，现在改为“is_confident == true 才进 LLM”
      - 位置：Test_SearchCustomer.yml:1188
  - 连线重排
      - if(doc_ready=true) 和 doc_ready赋值后 都先走 Query Normalize 再进 知识检索
      - 知识检索 -> Result Validate -> 条件分支
      - 关键边位置：Test_SearchCustomer.yml:401 / Test_SearchCustomer.yml:424 / Test_SearchCustomer.yml:436 / Test_SearchCustomer.yml:448
 
  我做过的验证
 
  - YAML 可解析，图结构完整（30 nodes / 30 edges）。
  - 三段代码节点都能 compile 通过。
  - 用 input2.json 做了本地模拟：
      - 入库文本能产出 企业名称/检索增强 等字段。
      - 查询纠错可把“实液/有现公司”纠为“实业/有限公司”候选。
      - 二次校验在目标企业存在时返回 is_confident=true，召回偏离时会返回 false。
 
  备份
 
  - 原文件已备份到：Test_SearchCustomer.yml.bak_homophone_20260306
 
  1. 你现在可以直接导入这版 YAML，优先用 5-10 组语音同音词做回归（如“实液/实业”“有现/有限”“科纪/科技”）。        
  2. 如果你愿意，我可以再按你真实客户名再补一轮 HOMOPHONE_HINTS 词表（行业词+重点客户名），把命中率再往上拉一档。
