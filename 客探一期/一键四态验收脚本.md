• 已给你落好“一键四态验收脚本”，包含你要的完整链路：
  1. success / duplicate / degraded 自动验收
  2. failed 动态验收（临时改坏 dataset_api_key）
  3. 失败告警 webhook 动态验收（支持自动创建 webhook.site）
  4. 自动回滚配置并做回滚后 smoke success
  5. 自动产出 JSON + Markdown + SSE 原始证据

  脚本文件

  - run_4state_acceptance.ps1

  说明文档

  - ACCEPTANCE_RUNNER.md
  我已做的本地自检
 
  - 已执行三态脚本跑通，报告成功生成（脚本逻辑与解析链路 OK）
  - 示例报告目录：
      - acceptance_report.json
      - acceptance_report.md

  注意：Windows 终端直接传中文参数时可能会编码成 ?，文档里已写 chcp 65001 规避方式。

  ———

  你现在可直接跑：
  powershell -ExecutionPolicy Bypass -File E:\KeTan_Dify\Test_VisitSummary\run_4state_acceptance.ps1 `
    -AppId "b612bb02-2931-4258-84e7-7943272da7fd" `
    -AppCode "Ap81eqbADvs6XwVA" `
    -AppPassport "<你的passport>" `
    -EnableFailedDynamic `
    -CsrfToken "<你的csrf>" `
    -ConsoleCookie "<完整cookie请求头>" `
    -SuccessQuery "查询东莞市思纳克材料科技有限公司联系人信息" `
    -DegradedQuery "查询银河量子科技联系人信息"
 

  如果你愿意，我下一步可以直接替你执行这条“四态+动态告警+回滚”的正式验收命令，并把本轮报告结论（run_id/节点命中/告警payload）贴给你。


• 已完成，正式跑了一轮“四态 + 失败告警动态验收 + 自动回滚”，结果通过。

  本轮结果

  1. 总结：5/5 passed，脚本退出码 0
  2. success：a55956dd-75d7-4569-aac5-f20ab35e8354（succeeded）
  3. duplicate：2893bfff-2d4a-440d-90b0-dfd47a2eb638（succeeded）
  4. degraded：2d3bf447-3641-495e-9f62-49779b444257（succeeded）
  5. failed：ba8a0556-b3b8-4ef3-a63f-a26773ac1b17（partial-succeeded，符合 fail-branch 预期）
  6. rollback_success：17323d2b-4e11-408b-b468-d8b6e82e906d（succeeded）  
  7. 失败告警 webhook 动态命中：
      - webhook: https://webhook.site/dbb28604-ae7d-4742-9c52-66a2b83b309e
      - request_uuid: 667bb6ef-cdc1-409d-9051-de85902bfb7e
      - trace_id: 20260305020011-cc872228
      - employee_code: 000004

  报告与证据文件

  - 总报告 MD: acceptance_report.md
  - 总报告 JSON: acceptance_report.json
  - success SSE: success.sse.txt
  - duplicate SSE: duplicate.sse.txt
  - degraded SSE: degraded.sse.txt
  - failed SSE: failed.sse.txt
  - 回滚后 success SSE: rollback_success.sse.txt
 
  我已同时修好的脚本点
 
  1. chat-messages 请求改为 UTF-8 字节提交，避免中文被编码成 ?
  2. console API 改为 WebRequestSession + CookieContainer，不再依赖无效的 Cookie 头直塞
  3. 默认查询改为 base64 解码内置中文，降低终端编码影响
  4. 文档已重写为纯 ASCII 说明

  脚本与文档：

  - run_4state_acceptance.ps1
  - ACCEPTANCE_RUNNER.md
 
 
  如果你要，我下一步可以把这个脚本接进 Windows 任务计划（每天定时跑），并把 acceptance_report.md 自动推送到企业微信/飞书。
