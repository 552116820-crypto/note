❯ [root@AI-appliancce-server ~]# ls
  check_mysql.py  churn-warning-engine  dify  mysql80-community-release-el8-6.noarch.rpm  wecom-deepexi-aibot
  ，这是服务器上的文档，我是否应该新建个文档，专门放churn-warning-engine和后续部署的smart_lead_generation。churn-warni
  ng-engine和smart_lead_generation是否应该放在一个项目文档中开发，因为churn-warning-engine不直接给前端提供接口，只是把
  预警数据传给crm数据库，而smart_lead_generation则是给前端提供接口的。回答我的问题