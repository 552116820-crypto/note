```shell
5417	000068	/api/v1/leads/score-and-info	{"lat": 999, "lng": 117.86, "page": 1, "page_size": 20, "radius_km": 60, "employee_code": "000068"}	1002		2.000	2026-06-17 16:16:19
5418	000068	/api/v1/leads/score-and-info	{"lat": -90.0001, "lng": 117.86, "page": 1, "page_size": 20, "radius_km": 60, "employee_code": "000068"}	1002		0.000	2026-06-17 16:16:19
5419	000068	/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 180.0001, "page": 1, "page_size": 20, "radius_km": 60, "employee_code": "000068"}	1002		0.000	2026-06-17 16:16:19
5420	000068	/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 117.86, "page": 1, "page_size": 20, "radius_km": 0, "employee_code": "000068"}	1002		0.000	2026-06-17 16:16:19
5421	000068	/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 117.86, "page": 1, "page_size": 20, "radius_km": -5, "employee_code": "000068"}	1002		0.000	2026-06-17 16:16:19
5422	000068	/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 117.86, "page": 1, "page_size": 20, "radius_km": 100001, "employee_code": "000068"}	1002		0.000	2026-06-17 16:16:19
5423	000068	/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 117.86, "page": 0, "page_size": 20, "radius_km": 60, "employee_code": "000068"}	1002		0.000	2026-06-17 16:16:19
5424	000068	/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 117.86, "page": 1, "page_size": 0, "radius_km": 60, "employee_code": "000068"}	1002		0.000	2026-06-17 16:16:19
5425		/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 117.86, "page": 1, "page_size": 20, "radius_km": 60, "employee_code": ""}	1002		0.000	2026-06-17 16:16:19
5426		/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 117.86, "page": 1, "page_size": 20, "radius_km": 60, "employee_code": "   "}	1002		0.000	2026-06-17 16:16:19
5427	000068	/api/v1/leads/score-and-info	{"lat": "abc", "lng": 117.86, "page": 1, "page_size": 20, "radius_km": 60, "employee_code": "000068"}	1002		0.000	2026-06-17 16:16:19
5428	000068	/api/v1/leads/score-and-info	{"lng": 117.86, "page": 1, "page_size": 20, "radius_km": 60, "employee_code": "000068"}	1002		0.000	2026-06-17 16:16:19
5429		/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 117.86, "page": 1, "page_size": 20, "radius_km": 60}	1002		0.000	2026-06-17 16:16:19
5430	000068	/api/v1/leads/score-and-info	{"lat": 90, "lng": 117.86, "page": 1, "page_size": 20, "radius_km": 60, "employee_code": "000068"}	0	0	0.000	2026-06-17 16:16:19
5431	000068	/api/v1/leads/score-and-info	{"lat": -90, "lng": 117.86, "page": 1, "page_size": 20, "radius_km": 60, "employee_code": "000068"}	0	0	0.000	2026-06-17 16:16:19
5432	000068	/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 180, "page": 1, "page_size": 20, "radius_km": 60, "employee_code": "000068"}	0	0	0.000	2026-06-17 16:16:19
5433	000068	/api/v1/leads/score-and-info	{"lat": 39.96, "lng": -180, "page": 1, "page_size": 20, "radius_km": 60, "employee_code": "000068"}	0	0	1.000	2026-06-17 16:16:19
5434	000068	/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 117.86, "page": 1, "page_size": 20, "radius_km": 100000, "employee_code": "000068"}	0	0	0.000	2026-06-17 16:16:19
5435	000068	/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 117.86, "page": 1, "page_size": 500, "radius_km": 60, "employee_code": "000068"}	0	0	0.000	2026-06-17 16:16:19
5436	000068	/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 117.86, "page": 1, "page_size": 20, "radius_km": 60, "employee_code": " 000068 "}	0	0	1.000	2026-06-17 16:16:19
5437	000068	/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 117.86, "page": 1, "page_size": 500, "radius_km": 60, "employee_code": "000068"}	0	0	0.000	2026-06-17 16:16:19
5438	000068	/api/v1/leads/skip	{"customer_id": "abc", "employee_code": "000068"}	1002		0.000	2026-06-17 16:16:19
5439	000068	/api/v1/leads/skip	{"customer_id": "", "employee_code": "000068"}	1002		0.000	2026-06-17 16:16:19
5440	000068	/api/v1/leads/skip	{"customer_id": "1.5", "employee_code": "000068"}	1002		0.000	2026-06-17 16:16:19
5441		/api/v1/leads/skip	{"customer_id": "198", "employee_code": ""}	1002		0.000	2026-06-17 16:16:19
5442		/api/v1/leads/skip	{"customer_id": "198", "employee_code": "   "}	1002		0.000	2026-06-17 16:16:19
5443	000068	/api/v1/leads/skip	{"employee_code": "000068"}	1002		0.000	2026-06-17 16:16:19
5444		/api/v1/leads/skip	{"customer_id": "198"}	1002		0.000	2026-06-17 16:16:19
5445		/api/v1/leads/skip	{}	1002		0.000	2026-06-17 16:16:19
5446	000068	/api/v1/leads/skip	{"customer_id": "198", "employee_code": "000068"}	0		1.000	2026-06-17 16:16:19
5447	000068	/api/v1/leads/skip	{"customer_id": 198, "employee_code": "000068"}	0		1.000	2026-06-17 16:16:19
5448	000068	/api/v1/leads/timeline	{"customer_id": "abc", "employee_code": "000068"}	1002		1.000	2026-06-17 16:16:19
5449	000068	/api/v1/leads/timeline	{"customer_id": "", "employee_code": "000068"}	1002		0.000	2026-06-17 16:16:19
5450	000068	/api/v1/leads/timeline	{"employee_code": "000068"}	1002		0.000	2026-06-17 16:16:19
5451		/api/v1/leads/timeline	{"customer_id": "3599"}	0		0.000	2026-06-17 16:16:19
5452		/api/v1/leads/timeline	{"customer_id": "3599", "employee_code": "   "}	0		1.000	2026-06-17 16:16:19
5453	000068	/api/v1/leads/timeline	{"customer_id": "3599", "employee_code": " 000068 "}	0		0.000	2026-06-17 16:16:19
5454		/api/v1/leads/recommend-reason	{"customer_id": "3599", "employee_code": ""}	1002		1.000	2026-06-17 16:16:19
5455		/api/v1/leads/recommend-reason	{"customer_id": "3599", "employee_code": "   "}	1002		0.000	2026-06-17 16:16:19
5456	000068	/api/v1/leads/recommend-reason	{"customer_id": "abc", "employee_code": "000068"}	1002		0.000	2026-06-17 16:16:19
5457		/api/v1/leads/recommend-reason	{"customer_id": "3599"}	1002		0.000	2026-06-17 16:16:19
5458	000068	/api/v1/leads/recommend-reason	{"employee_code": "000068"}	1002		0.000	2026-06-17 16:16:19
5459	000068	/api/v1/leads/recommend-reason	{"customer_id": "3599", "employee_code": "000068"}	1002		0.000	2026-06-17 16:16:19
5460	000068	/api/v1/leads/recommend-reason	{"customer_id": "3599", "employee_code": "000068"}	1002		0.000	2026-06-17 16:16:19
5461	000068	/api/v1/leads/recommend-reason	{"customer_id": "3599", "employee_code": "000068"}	0		0.000	2026-06-17 16:16:19
5462	000068	/api/v1/leads/recommend-reason	{"customer_id": "3599", "employee_code": "000068"}	0		0.000	2026-06-17 16:16:19
5463	000068	/api/v1/leads/recommend-reason	{"customer_id": "3599", "employee_code": "000068"}	0		0.000	2026-06-17 16:16:19
5464		/api/v1/leads/score-and-info		1002		0.000	2026-06-17 16:16:19
5465		/api/v1/leads/score-and-info	[]	1002		0.000	2026-06-17 16:16:19
5466		/api/v1/leads/score-and-info	"x"	1002		0.000	2026-06-17 16:16:19
5467		/api/v1/leads/score-and-info	123	1002		0.000	2026-06-17 16:16:19
5468	000068	/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 117.86, "page": 1, "page_size": 20, "radius_km": 60, "industry_ids": [43], "employee_code": "000068"}	0	0	1.000	2026-06-17 16:16:19
5469	000068	/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 117.86, "page": 1, "page_size": 20, "radius_km": 60, "industry_ids": [43, 67], "employee_code": "000068"}	0	0	0.000	2026-06-17 16:16:19
5470	000068	/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 117.86, "page": 1, "page_size": 20, "radius_km": 60, "industry_ids": [], "employee_code": "000068"}	0	0	0.000	2026-06-17 16:16:19
5471	000068	/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 117.86, "page": 1, "page_size": 20, "radius_km": 60, "employee_code": "000068", "industry_names": ["数控机床"]}	0	0	1.000	2026-06-17 16:16:19
5472	000068	/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 117.86, "page": 1, "page_size": 20, "radius_km": 60, "employee_code": "000068", "industry_names": []}	0	0	0.000	2026-06-17 16:16:19
5473	000068	/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 117.86, "page": 1, "page_size": 20, "radius_km": 60, "industry_ids": ["x"], "employee_code": "000068"}	1002		0.000	2026-06-17 16:16:19
5474	000068	/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 117.86, "page": 1, "page_size": 20, "radius_km": 60, "industry_ids": "notalist", "employee_code": "000068"}	1002		0.000	2026-06-17 16:16:19
5475	000068	/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 117.86, "page": 1, "page_size": 20, "radius_km": 60, "employee_code": "000068", "industry_names": "notalist"}	1002		0.000	2026-06-17 16:16:19
5476	000068	/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 117.86, "page": 1, "page_size": 20, "radius_km": 60, "employee_code": "000068"}	1503		1.000	2026-06-17 16:16:19
5477	000068	/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 117.86, "page": 1, "page_size": 20, "radius_km": 60, "employee_code": "000068"}	1003		0.000	2026-06-17 16:16:19
5478	000068	/api/v1/leads/skip	{"customer_id": "198", "employee_code": "000068"}	1505		1.000	2026-06-17 16:16:19
5479	000068	/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 117.86, "page": 1, "page_size": 20, "radius_km": 60, "employee_code": "000068"}			0.000	2026-06-17 16:16:19
5480		/api/v1/leads/timeline	{"customer_id": "3599"}	0		0.000	2026-06-17 16:16:19
5481	000068	/api/v1/leads/recommend-reason	{"customer_id": "3599", "employee_code": "000068"}	0		1.000	2026-06-17 16:16:19
5482	000068	/api/v1/leads/recommend-reason	{"customer_id": "3599", "employee_code": "000068"}	0		1.000	2026-06-17 16:16:19
5483		/api/v1/leads/score-and-info	{}	1002		0.000	2026-06-17 16:16:19
5484		/api/v1/leads/skip	{}	1002		0.000	2026-06-17 16:16:19
5485		/api/v1/leads/timeline	{}	1002		0.000	2026-06-17 16:16:19
5486		/api/v1/leads/recommend-reason	{}	1002		0.000	2026-06-17 16:16:19
5487	000068	/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 117.86, "page": 1, "page_size": 20, "radius_km": 60, "employee_code": "000068"}	0	0	1.000	2026-06-17 16:16:19
5488		/api/v1/leads/timeline	{"customer_id": "3599"}	0		0.000	2026-06-17 16:16:19
5489	000068	/api/v1/leads/recommend-reason	{"customer_id": "3599", "employee_code": "000068"}	0		0.000	2026-06-17 16:16:19
5490	000068	/api/v1/leads/skip	{"customer_id": "198", "employee_code": "000068"}	0		0.000	2026-06-17 16:16:19
5491	000068	/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 117.86, "page": 1, "page_size": 20, "radius_km": 60, "employee_code": "000068"}	1503		0.000	2026-06-17 16:16:19
5492	000068	/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 117.86, "page": 1, "page_size": 20, "radius_km": 60, "employee_code": "000068"}	1003		0.000	2026-06-17 16:16:19
5493	000068	/api/v1/leads/skip	{"customer_id": "198", "employee_code": "000068"}	1505		0.000	2026-06-17 16:16:19
5494	000068	/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 117.86, "page": 1, "page_size": 20, "radius_km": 60, "employee_code": "000068"}			0.000	2026-06-17 16:16:19
5495	000068	/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 117.86, "page": 1, "page_size": 20, "radius_km": 60, "employee_code": "000068"}	1001		0.000	2026-06-17 16:16:19
5496	000068	/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 117.86, "page": 1, "page_size": 20, "radius_km": 60, "employee_code": "000068"}	1004		0.000	2026-06-17 16:16:19
5497	000068	/api/v1/leads/score-and-info	{"lat": 39.0, "lng": 117.0, "page": 1, "page_size": 20, "radius_km": 60, "employee_code": "000068"}	0	4	1.000	2026-06-17 16:16:19
5498	000068	/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 117.86, "page": 1, "page_size": 20, "radius_km": 60, "employee_code": "000068"}	0	0	0.000	2026-06-17 16:16:19
5499	000068	/api/v1/leads/skip	{"customer_id": "1 OR 1=1", "employee_code": "000068"}	1002		0.000	2026-06-17 16:16:19
5500	000068	/api/v1/leads/skip	{"customer_id": "1; DROP TABLE crm_customer", "employee_code": "000068"}	1002		0.000	2026-06-17 16:16:19
5501	000068	/api/v1/leads/skip	{"customer_id": "1' OR '1'='1", "employee_code": "000068"}	1002		0.000	2026-06-17 16:16:19
5502	000068	/api/v1/leads/skip	{"customer_id": "1 UNION SELECT 1", "employee_code": "000068"}	1002		0.000	2026-06-17 16:16:19
5503	1 OR 1=1	/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 117.86, "page": 1, "page_size": 20, "radius_km": 60, "employee_code": "1 OR 1=1"}	0	0	0.000	2026-06-17 16:16:19
5504	1; DROP TABLE cr	/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 117.86, "page": 1, "page_size": 20, "radius_km": 60, "employee_code": "1; DROP TABLE crm_customer"}	0	0	0.000	2026-06-17 16:16:19
5505	1' OR '1'='1	/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 117.86, "page": 1, "page_size": 20, "radius_km": 60, "employee_code": "1' OR '1'='1"}	0	0	0.000	2026-06-17 16:16:19
5506	1 UNION SELECT 1	/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 117.86, "page": 1, "page_size": 20, "radius_km": 60, "employee_code": "1 UNION SELECT 1"}	0	0	0.000	2026-06-17 16:16:19
5507	000068	/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 117.86, "employee_code": "000068"}	1002		0.000	2026-06-17 16:16:19
5508	000068	/api/v1/leads/score-and-info	{"lat": 39.96, "lng": 117.86, "employee_code": "000068"}	1002		0.000	2026-06-17 16:16:19
```

# Claude Code 对话记录

- **来源**: Claude Code
- **会话**: `e044a3e8-8f55-4fa4-880e-af5a3925d78a`
- **范围**: Turn 1-12
- **导出时间**: 2026-06-17 17:54:29

---

### Turn 1  *(2026-06-17 08:05)*

**User:**

5415    000068    /api/v1/leads/score-and-info    {"lat": 39.96, "lng": 117.86, "employee_code": "000068"}    1002        0    2026-06-17 10:25:57
上面一条lead_api_log日志表上的最后一条记录，为什么我在服务器上请求了接口，日志没记上[root@AI-appliancce-server amer_sales_assist]# curl -X POST "http://localhost:8080/api/v1/leads/timeline" \
> -H "Content-Type: application/json" \
> -d '{ "customer_id": "3599" }'。我在本地http://8.138.104.14:8080/api/v1/leads/timeline，也没记上呢？我好像明白了，amer_sales_assist就没接上日志表，我启动smart_lead_generation这个服务再请求就有了，但是smart_lead_generation只是合并前的项目啊，我要amer_sales_assist这个合并后的项目服务请求接口能写入日志啊。
还有一个问题86    000068    73308    skip    2026-06-17 16:00:27    2026-06-17 16:00:27 这是lead_skip表的最后一条记录，显示73308已经skip了，但是http://8.138.104.14:8080/api/v1/leads/score-and-info请求返回的结果没有把73308skip掉

**Assistant:**

I'll investigate both issues. Let me start by exploring how API logging and skip logic work in the merged `amer_sales_assist` project.

Key findings already emerging. Let me read the critical files in full.

Both issues are becoming clear. Let me read the remaining pieces — skip repo, log pool, cache store, router, and the env example.

Let me confirm the exact config defaults — the API-logging kill-switch, the cache TTL, and the log-DB settings.

Let me confirm there's no cache invalidation tied to the skip action (the last piece for issue #2).

I now have a complete, confident picture of both issues. Let me do one quick check of the local `.env` (just the logging flag, not secrets) to confirm the hypothesis.

The local dev `.env` has it **on** (`=1`) — so the server's value is the unknown that matters. Let me check how `docker compose` passes this flag into the container, since that's a common breakage point after a merge.

I've traced both issues to root cause. Here's what's going on.

## 问题一：amer_sales_assist 请求没写进 `lead_api_log`

**不是"没接上"——代码是全接好的**（中间件 `ApiLogMiddleware` + `lead_api_log_repo` + LOG_DB 都在），卡在一个 kill-switch 开关上：

- `app/lead/middleware/api_log.py:256`：中间件第一件事就是 `if ... not settings.enable_api_logging: 直接放行`，整条日志逻辑跳过。
- `app/lead/config/settings.py:119`：`ENABLE_API_LOGGING` **默认 `0`（关）**；`.env.example:117` 也是 `0`。
- compose 用 `env_file: .env` 把整份 `.env` 注进容器（`docker-compose.yml:33`），所以**真正生效的是服务器上那份 `.env`** —— 本机 dev 的 `.env` 虽是 `=1`，跟服务器无关。

**关键证据链**：你那条 16:00:27 的 skip 成功写进了 `lead_skip`（说明跑着的这个服务**能连上 LOG_DB**），但它**没有**出现在 `lead_api_log` 里（skip 也在日志白名单内，`api_log.py:29`）。`lead_api_log` 最后一条还停在 10:25:57。→ 现在answer :8080 的这个服务，LOG_DB 连得上、但 **API 日志开关是关的**。这正是 amer_sales_assist 服务器 `.env` 里 `ENABLE_API_LOGGING` 没设成 `1`（或没这行）的表现。

**修复（在服务器上，我远程改不了）**：
1. 服务器 amer_sales_assist 的 `.env` 里设 `ENABLE_API_LOGGING=1`；
2. 重启 api：`docker compose up -d`（或 `docker compose restart api celery`）；
3. 验证：`curl --noproxy '*' http://127.0.0.1:8080/api/v1/leads/score-and-info ...` 后查 `lead_api_log` 有没有新行。
4. 顺便确认 :8080 现在确实是 **amer_sales_assist** 在answer而不是旧的 smart_lead_generation：`docker compose ps`（看是哪个项目起着）。`timeline` 本身在白名单里（`api_log.py:30`），路径没问题，不用担心。

> 题外：那条 10:25:57 的记录 `biz_code=1002`、`result_total` 空、`cost_ms=0`，是一次**业务错误返回**（早退路径），不是正常结果——和日志机制无关，但 000068 这个工号查 score-and-info 报 1002 你可以另外留意下。

## 问题二：73308 已 skip，但 score-and-info 还返回它

这其实是**两件事叠加，skip 没丢**：

1. **设计上 skip = 沉底，不是删除**（`lead_service.py:63-71` 的 `partition_by_skip`：未看过的排前、跳过的排到**末尾**）。73308 仍会出现，只是被压到列表最后，`pagination.skipped_total` 会 +1。`leads.py:28` 的注释也写了：「跳过在下次缓存重算时沉底生效；本次会话由前端乐观隐藏卡片」——你用 curl 裸调绕过了前端的乐观隐藏，所以还看得到。
2. **缓存没失效**：score-and-info 的结果按 `(employee_code, lng, lat, radius)` 整块缓存，TTL **300s**（`cache/store.py:57`）。而 skip 集合只在 `_compute_blob` 的 **cache-miss 路径**里读一次（`lead_service.py:130-135`），`POST /skip` **不会**让这个缓存失效（`leads.py:29-32` 只写库就返回）。所以 16:00:27 之前算好的 blob，在 300s 内、同样经纬度/半径的重复请求一直命中旧缓存——73308 连"沉底"都还没发生。

**自查**：看返回里的 `pagination.skipped_total`。`=0` → 命中的是旧缓存（skip 还没生效）；`≥1` → skip 已生效，73308 只是被沉到末尾了（按设计如此）。

skip 的读路径不会失败（上面已证 LOG_DB 连得上），所以问题二纯粹是「缓存陈旧 + 沉底而非删除」，要不要改取决于你想要的语义——这个我来问你定。

API Error: 529 Overloaded. This is a server-side issue, usually temporary — try again in a moment. If it persists, check https://status.claude.com.

No response requested.

---

### Turn 2  *(2026-06-17 08:28)*

**User:**

继续啊

**Assistant:**

No response requested.

---

### Turn 3  *(2026-06-17 08:34)*

**User:**

继续

**Assistant:**

明白,问题二按你的决定**不动代码**(后端沉底+TTL 后生效,前端乐观隐藏)。那现在只剩**问题一**有一个实打实的动作——在服务器上把日志开关打开。你已经 SSH 在 `AI-appliancce-server` 的 `amer_sales_assist` 目录里,直接跑:

**1. 看当前值**
```bash
grep ENABLE_API_LOGGING .env
```

**2. 设成 1(这行幂等:有就改、没有就追加)**
```bash
grep -q '^ENABLE_API_LOGGING=' .env \
  && sed -i 's/^ENABLE_API_LOGGING=.*/ENABLE_API_LOGGING=1/' .env \
  || echo 'ENABLE_API_LOGGING=1' >> .env
```

**3. 重建 api 容器(env_file 改了必须 recreate,restart 不够)**
```bash
docker compose up -d --force-recreate api
docker compose ps          # 确认 Up 的是 amer_sales_assist 这套,不是旧的 smart_lead_generation
```
> 关键:确认 :8080 现在确实归 amer_sales_assist 的 compose 栈。两个项目都想绑 :8080,同一时间只能有一个;你之前"启 smart_lead_generation 才有日志"就是因为那会儿 :8080 是旧项目在answer。

**4. 验证写库**
```bash
curl --noproxy '*' -X POST http://127.0.0.1:8080/api/v1/leads/timeline \
  -H "Content-Type: application/json" -d '{"customer_id":"3599"}'
```
然后查 `lead_api_log`(LOG_DB = 8.138.104.14:23356,库 `amer_sales_assist`)应出现一条 `endpoint=/api/v1/leads/timeline`、时间是刚才的新行。

---

**问题二想自己确认"按设计在工作"**(不用改代码):skip 后别立刻用同样经纬度/半径请求(会命中 ≤300s 的旧缓存)。等 5 分钟过 TTL,或把 `radius_km`/经纬度微调一下触发重算,再看返回里的 `pagination.skipped_total`——应 ≥1,且 73308 被压到列表最末尾(注意是沉底,不是消失)。

> 提醒:你们 `REDIS_URL` 被 compose 钉在 `redis://redis:6379/2`,缓存在 Redis 里、**重启 api 不会清**,所以要靠 TTL 到期或换 key 才生效,别指望重启服务来验证。

两个问题到此都闭环了:问题一是服务器 `.env` 的开关(上面 4 步),问题二是确认而非修复。还要我做别的吗?

---

### Turn 4  *(2026-06-17 08:39)*

**User:**

16:00:27 之前算好的 blob，在 300s 内、同样经纬度/半径的重复请求一直命中旧缓存——73308，连"沉底"都还没发生。我都忘记我为什么要这样设计了？你能告诉我吗？我改了ENABLE_API_LOGGING=1，最新一条5509成功录入，日志表上有个问题，为什么2026-06-17 16:16:19这个时间点一下就生成了5417到5508的日志

**Assistant:**

Good — logging's live now (5509 in). Let me confirm one detail about the table before I explain the timestamp burst.

两个都答你。第一个是你自己当初的设计取舍,第二个是批量落库的正常表现。

## 一、skip 为什么是"沉底+缓存重算才生效",不是即时删除

你的代码注释其实把当初的理由都记下来了,我帮你串起来——是**三个有意为之的取舍**叠在一起:

**1. 沉底,不是删除**(`lead_service.py:63-71` `partition_by_skip`)
skip 是"降权",不是"拉黑"。沉底保留了 `unseen_total`(还有几个新客户)和 `skipped_total`(已跳过)两个数(`lead_service.py:198-199`),前端能显示"还有 N 个新客户";而且 `total` 不缩水、客户还能翻到底部找回来——非破坏性、可逆。删除就把这些都丢了。

**2. 把分层"冻进"缓存 blob,只在 cache-miss 时读一次 skip,不在每次读路径读**(`lead_service.py:128-129`、`cache/store.py:3-5`)
这是核心。注释原话:**"冻进缓存 blob,翻页只切这个序"** + **"增强功能不拖垮核心读路径"**。三个好处:
- **翻页一致性**:重的 geo+打分(冷算 ~7-10s、候选上限 2-3 万)每个 key 只算一次,排序定死后翻页是**纯内存切片**(`lead_service.py:1-6` docstring)。如果 skip 即时生效,你翻到第 2 页时列表会重排,可能漏看或重复。
- **不给热读路径加远程依赖**:LOG_DB 是异地、故意短超时的库(`db/log_pool.py:1-5`)。只在冷算时读一次 skip,把这个"可能连不上"的远程查询限制在每个 key 每 300s 一次,而不是每次请求、每次翻页都打一遍。
- **连不上就降级**:`lead_service.py:130-135` 读 skip 失败就当空集、推荐照常。

**3. 本次会话的即时性,交给前端**(`leads.py:28` 原注释:**"跳过在下次缓存重算时沉底生效;本次会话由前端乐观隐藏卡片"**)
你点 skip,前端立刻把那张卡片隐藏掉,所以后端不需要即时重排。后端只负责"下次重算时沉底"。对一个**走动中的业务员**,经纬度一变就是新 key、立刻重算,沉底自然很快就生效。

**所以那句 300s 陈旧不是 bug,是这套"重算一次、缓存到底"设计的明码标价**:只有"原地不动、300s 内、同经纬度/半径重复查"这一种情形会看到滞后——而真实业务里这种情形很少,且前端已经把它遮住了。你用 curl 裸调正好踩中了这个唯一的缝。

## 二、为什么 16:16:19 一下冒出 5417→5508(~92 条)

这是**批量落库在按设计工作**,不是补写、也不是重复写。两条机制叠加:

**1. `created_at` 是落库时间,由库填,不是请求时间**
建表是 `created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP`(`017_create_lead_tables.sql:28`),INSERT 语句根本不带这列(`lead_api_log_repo.py:11-14`),所以是 MySQL 在 **INSERT 那一刻**盖的时间戳。

**2. 日志不是一条一写,是攒批一次性多行 INSERT**(`middleware/api_log.py`)
请求只把日志 O(1) 丢进内存队列,后台 flusher 攒到一批(最多 200 条 `api_log_batch_size`)用**一条多行 INSERT** 落库。而 MySQL 对**同一条 INSERT 语句里的所有行只取一次 `CURRENT_TIMESTAMP`**,同时 `AUTO_INCREMENT` 给这一批分配**连续的 id 段**。

**→ 5417 到 5508 连号、且时间戳全是 16:16:19,这正是"一次批量 flush 把 ~92 条一锅端进去"的指纹**。不是 92 次分别写入,是 1 次 INSERT 写了 92 行。

**那为什么会有 ~92 条挤在一秒里?** 几乎可以肯定是:你 `--force-recreate api` 的瞬间,API 重新起来,**所有客户端(灰度那 ~50 个业务员开着的前端 + 轮询)同时重连/重试**,一秒内涌进 ~92 个请求,被 flusher 合成一批。旧容器当时日志是关的(就是问题一),队列里没有积压,所以这批**全是重启后的真实新流量**,不是旧账补写。(5416 大概是重启后第一个零散请求,5509 是你那条 curl 验证。)

**想确认这批是真流量、不是某个重试死循环**,查一下分布:
```sql
SELECT endpoint, COUNT(*) n, COUNT(DISTINCT employee_code) emps, MIN(cost_ms), MAX(cost_ms)
FROM lead_api_log WHERE id BETWEEN 5417 AND 5508 GROUP BY endpoint;
```
多个 employee_code + 几个接口混着 + cost_ms 有真实耗时 = 重连风暴,正常;若全是同一个工号/同一接口刷屏,才需要查客户端重试逻辑。

**一个要记住的副作用**:`created_at` 是"批量 flush 时间"(比请求完成晚 ≤500ms,还会把一秒内的一批塌缩成同一秒),所以**别拿它当每条请求的精确到达时刻**做时序分析——它是落库时间,不是请求时间。要精确到达时间得另加列,现在没必要。

要我把这批 5417-5508 的分布拉出来看看吗?(需要你给我连 LOG_DB 的方式,或者你自己跑上面那条 SQL 贴结果。)

---

### Turn 5  *(2026-06-17 08:56)*

**User:**

/api/v1/leads/recommend-reason    14    1    0.000    1.000
/api/v1/leads/score-and-info    51    5    0.000    2.000
/api/v1/leads/skip    18    1    0.000    1.000
/api/v1/leads/timeline    9    1    0.000    1.000，给你结果，好像就是重起docker就有

---

### Turn 6  *(2026-06-17 09:00)*

**User:**

/api/v1/leads/recommend-reason    14    1    0.000    1.000
/api/v1/leads/score-and-info    51    5    0.000    2.000
/api/v1/leads/skip    18    1    0.000    1.000
/api/v1/leads/timeline    9    1    0.000    1.000，给你sql结果，这一批日志就是重起docker就有的

**Assistant:**

92 rows total (51+18+14+9) — matches 5417→5508 exactly. But the data tells a sharper story than "reconnect storm," and one number is worth checking. Let me verify whether anything in the stack self-fires these endpoints on startup before I attribute it to clients.

Now I see it — the app's startup lifespan does **not** self-call any endpoint (only warms the industry resolver), and `make smoke` hits score-and-info just once. But there are two scripts that hit all four endpoints. Let me read them to see which one's request profile matches your 51/18/14/9.

`http_smoke.py` is ruled out — it runs on its own port (8077), doesn't touch timeline/recommend-reason, and **deletes its own log rows** (line 227). So it can't be the source. The remaining candidate that hits all four endpoints is `load_test.py`. Let me check its profile and whether it cleans up.

That settles it. Both standalone scripts are eliminated — and the answer is the opposite of what it looks like: **docker restart isn't generating these logs, your real users are.**

## 先把"会自己造日志"的来源全排掉

- **App 启动**(`app/main.py:54-64`):只 warm 行业解析器(一次内部 DB 读),**不自调任何 HTTP 接口**。✗
- **`make smoke`**(`Makefile:60-72`):只打 1 次 score-and-info(工号 000068)+ healthz。给不出 51/18/14/9。✗
- **`http_smoke.py`**:跑在**自己的 8077 端口**、不碰 timeline/recommend-reason,而且**结束时把自己写的日志行全删掉**(`DELETE FROM lead_api_log WHERE id > pre_max_log_id`,line 227)。✗
- **`load_test.py`**:跑在**自己的 8078 端口**,是手动压测脚本,docker 起停根本不会触发它。✗

打在 docker 栈(:8080)上、四个接口都有、重启就冒出来的——只剩**真实前端流量**这一种。

## 决定性线索:cost_ms 全 ≤2ms(大多 0.000)

这条比时间戳更能说明问题。看你给的耗时:**timeline 最大 1ms、recommend-reason 最大 1ms**。但:
- timeline 要打 CRM 三个 HTTP 接口(超时 5s),真实成功调用**不可能 ≤1ms**;
- recommend-reason 要调 DeepSeek 大模型(超时 20s),真实调用是**秒级**;
- score-and-info 冷算(geo+打分)是 7-10s。

**它们能 ≤2ms 完成,只有一个可能:全部命中了 Redis 缓存。** 你做的是 `--force-recreate api`——**只重建了 api,redis 容器没动**,db2 里的缓存 blob(score-and-info 300s / timeline+reason 3600s)**整个活着**。所以这批请求全是"走暖缓存、零外部 I/O"的廉价命中,不是真冷算。(顺带:如果你 `docker compose restart` 把 redis 也重启了,这批 score-and-info 就会是 7-10s 而不是 0ms。)

## 所以真相是

docker 重启 → nginx/api 短暂不可用 → **那 ~50 个灰度业务员开着的前端,在 api 一恢复的瞬间集体重连/重试** → 一秒内涌进 92 个请求(走暖缓存,毫秒返回)→ 被 flusher 合成一条多行 INSERT → 连号 5417-5508 + 同一秒 16:16:19。

员工分布也对得上:**score-and-info 5 个工号** = 5 个业务员在列表页;**skip/timeline/reason 各 1 个工号** = 同一个业务员钻进了某个客户详情页(看时间轴+推荐理由)、并在候选里连点了 18 次 skip。这是很自然的真实使用画像,不是脚本(脚本用固定测试号)。

**关键:这些流量一直都在,只是你今天才把 `ENABLE_API_LOGGING` 打开,第一次"看见"了这个重连脉冲——以前它一直静默发生。**

## 一条 SQL 一锤定音

想确认是真用户还是某人跑了脚本,看工号和业务码:
```sql
SELECT employee_code, endpoint, biz_code, result_total, cost_ms
FROM lead_api_log WHERE id BETWEEN 5417 AND 5508 ORDER BY id;
```
- 工号是你的**真实业务员号**、`biz_code=0`、score-and-info 的 `result_total` 非空 → 确认是前端重连流量(暖缓存命中),正常、健康。
- 若出现 `ZZZZZZ` / `000424` 这种 → 那是有人手跑了 `http_smoke`/`load_test`(用真实 LOG_DB),不是 docker 干的。

**唯一值得多看一眼的是那 18 条 skip**——它们是真写库(`lead_skip` 真多了行)。upsert 幂等,重连重放同一个 skip 不会重复,所以无害;但你可以扫一眼那 18 个 customer_id 是不是那个业务员**有意**跳的,排掉前端"重连把待发 skip 队列一次性补发"里夹带误点的可能。

要我把这 92 行按工号拉出来你看看是不是真号吗?给我连 LOG_DB 的方式,或你自己跑上面那条贴结果都行。

---

### Turn 7  *(2026-06-17 09:07)*

**User:**

D:\note\智能拓客（第二阶段）\15.部署后发现并改进问题\log表上异常日志.md，这92行日志在这个文档了，你看一下

**Assistant:**

This is the decisive evidence — and it overturns my earlier guess. These are **not real users**. Let me confirm whether this exact battery lives in the repo so I can name the source.

Found the smoking gun. The battery matches three **pytest files** — `tests/lead/test_api_boundary.py`, `test_request_schema_bounds.py`, `test_security_surface.py`. These aren't scripts hitting :8080; they're the unit test suite. That changes everything. Let me confirm how they leak into the production log table.

This is now fully explained — and it's more interesting than "docker restart." Let me read the security test to confirm one detail (whether the skip writes also leaked) before I lay it out.

Confirmed — and I owe you a correction: my "real users reconnecting" theory was **wrong**. These 92 rows are your **pytest suite leaking into the production log table**. Here's the real story.

## 这 92 行是单元测试,不是真实流量

请求体本身就是铁证——没有真实用户会这样调接口:
- **边界值**:`lat:999`、`lat:-90.0001`、`lng:180.0001`、`radius_km:-5/0/100001`、`page:0`、`page_size:0/500`
- **类型错误**:`lat:"abc"`、`industry_ids:"notalist"`、`customer_id:"1.5"`
- **空/空白/缺字段**:`employee_code:""`、`"   "`、`" 000068 "`(测 strip)、整个 body 是 `[]`/`"x"`/`123`/`{}`
- **同参数却返回不同 code**(5476→1503、5477→1003、5495→1001、5496→1004、5498→0):这只可能是 **monkeypatch 强行注入各错误路径**,真实服务做不到
- **5499-5506 是 SQL 注入测试**:`"1; DROP TABLE crm_customer"`、`"1 OR 1=1"`、`"1' OR '1'='1"`、`"1 UNION SELECT 1"`

我之前说的"5 个不同工号"也是误读——其实是 `000068` + 那 4 个注入字符串,不是 5 个业务员。

来源就是这三个测试文件:`tests/lead/test_api_boundary.py`、`test_request_schema_bounds.py`、`test_security_surface.py`。

## 为什么测试会写进生产库(漏写机制)

这三个文件用 **`TestClient(app)` 打真实的 app**(`test_security_surface.py:36` 用 `client` fixture = `conftest.py:22` 的 `TestClient(app)`)。它们 monkeypatch 的是**业务层**(`lead_service.query_leads`、`lead_skip_repo.upsert_skip`),**但没有挡住 `ApiLogMiddleware`**。

而中间件是否落库只看一个开关:`settings.enable_api_logging`(`api_log.py:256`)。这三个文件没有动它——它们一直**靠默认值 `0`(关)来免疫**。

**你今天把服务器 `.env` 的 `ENABLE_API_LOGGING` 改成了 `1`,然后在那套 `.env`(`LOG_DB_HOST=8.138.104.14`)下跑了 pytest** → 中间件这下开着了 → 每个 TestClient 请求都被 enqueue → flusher 一次性批量 INSERT 进**生产 `lead_api_log`** → 连号 5417-5508、同一秒 16:16:19。

> 所以跟"重起docker"没直接关系:是"**打开日志开关 + 用生产 .env 跑测试**"。对比一下:`tests/lead/test_api_log_*.py` 是干净的——它们既 `monkeypatch enable_api_logging`、又把 `insert_logs` 桩成假的(`test_api_log_backpressure.py:29/65`),所以从不漏写。漏的恰恰是这三个"不关心日志"的文件。

## 好消息:安全测试全过了

- **注入全被挡死**:数字字段(skip 的 customer_id)在 schema 层 `int()` 强转就拒了 → 全返回 **1002**,repo 根本没碰(`test_security_surface.py:45-51`);自由文本字段(employee_code)原样透传给**参数化查询** → 返回 **code 0、result_total 0**(当成普通字符串查,查无此人),没有注入(`:55-62`)。参数化 + 强转两道防线都顶住了。
- **那些 1505/1503/1003 不是真故障**,是测试 monkeypatch 强行触发的错误路径,别当成生产 LOG_DB 抖动。
- **`lead_skip` 没被污染**:skip 的 `upsert_skip` 在测试里被桩掉了(`:48`),没有真写库。(保险起见你可以 `SELECT * FROM lead_skip WHERE employee_code='000068' AND customer_id=198;` 确认一下,预期为空。)

## 要做两件事

**1. 清掉这批测试污染行**(就在生产 `lead_api_log` 里):
```sql
DELETE FROM lead_api_log WHERE id BETWEEN 5417 AND 5508;
```

**2. 堵住漏写口子**(根因修复)。给 `tests/lead/conftest.py` 加一个 autouse fixture,让整套测试无论环境怎么设都不可能写真 LOG_DB:
```python
@pytest.fixture(autouse=True)
def _isolate_api_log(monkeypatch):
    """测试都走 TestClient(app),真实 ApiLogMiddleware 会跑。强制关日志 + 桩掉写库,
    确保没有任何测试因为环境里 ENABLE_API_LOGGING=1 就漏写真 LOG_DB。
    专测日志的 test_api_log_* 会在各自用例里自行打开并桩 insert_logs,不受影响。"""
    from app.lead.middleware import api_log
    monkeypatch.setattr(api_log.settings, "enable_api_logging", False)
    monkeypatch.setattr(api_log.lead_api_log_repo, "insert_logs", lambda recs: len(recs))
```
这样默认就是关的,`test_api_log_*` 那些用例自己 `monkeypatch ... True` 会在用例内覆盖、且它们本就桩了 `insert_logs`,527 基线不受影响。

**操作上**:在修好之前,别在生产服务器、带生产 `.env`(尤其 `ENABLE_API_LOGGING=1`)跑 pytest;部署后验证用 `make smoke`(它只打 1 次 score-and-info,不碰这些)。

要我现在就把这个 conftest fixture 加上、跑一遍 `pytest -q` 确认还是 527 全绿吗?

---

### Turn 8  *(2026-06-17 09:23)*

**User:**

堵住漏写口子是啥意思啊？难道不是测试写进了log表吗？

**Assistant:**

对,**就是测试写进了 log 表**——你理解完全没错。我说的"堵住漏写口子"就是指**让以后这事不再发生**,不是说没发生。区别在于:

## "测试写进了 log 表"——这本身是个 bug,不是正常现象

按 CLAUDE.md,你那 527 个测试是"**全部确定性、无外部依赖**"的。也就是说:**测试就不该碰任何真实数据库**,它应该在一个封闭沙箱里跑(把 DB、CRM、大模型全换成假的)。

可这次测试却把 92 行假数据(边界值、SQL 注入串)写进了**生产的 `lead_api_log`**——这说明测试的沙箱**有个没堵严的口子**。这就是"漏写口子"。

打个比方:测试本该是个密封水缸,所有进出口都该接到假管子上。但**日志中间件这条管子忘了接假的**,平时靠一个阀门(`ENABLE_API_LOGGING=0`)关着,所以一直没人发现漏。**你今天为了生产记日志把阀门拧开成 `=1`,测试又共用这个阀门设置,水就从那条没堵的管子漏进了生产水缸。**

## "堵住"指什么

在测试的初始化里(那个 conftest fixture)**强制把这条管子接成假的**——不管环境变量阀门是开是关,测试都物理上写不进真库。也就是把"靠默认值碰巧没漏"换成"主动焊死、永远不漏"。

## 关键澄清:这只改测试,不动生产

怕你误会——**我不是让你把刚开的生产日志关掉**。两码事:

| | 该不该写真 log 表 | 怎么处理 |
|---|---|---|
| **生产 app**(docker,真实业务员请求) | **该写** ✅ | `ENABLE_API_LOGGING=1` 保持不动 |
| **pytest 测试**(模拟请求) | **绝不该写** ❌ | conftest 里焊死,写不进 |

生产记日志是你要的功能,继续记;测试漏写是 bug,堵掉。

## 为什么必须堵,不能只删一次

如果只 `DELETE` 这 92 行、不堵口子:**以后每次在这台服务器(带生产 `.env`)跑一次 pytest,就又灌 92 行垃圾进生产表**。而且同一个没堵的口子,万一将来有个测试忘了桩业务层,漏进去的可能就不只是日志了。删是擦掉已漏的,堵是关掉源头——两件事都要做。

要我把这个 fixture 加上、跑一遍 `pytest -q` 确认还是 527 全绿吗?

---

### Turn 9  *(2026-06-17 09:28)*

**User:**

要。同时确认其他测试不会写进真实数据库

**Assistant:**

On it. First let me understand the existing test-isolation architecture before I add anything — I need to see how DB, CRM, and LLM are currently stubbed, and find every real-I/O choke point.

The conftest is deliberately **opt-in, never autouse** — tests avoid real DB by each monkeypatching the business layer; the api-log *middleware* was the one path nobody stubbed. To confirm there are no other leaks, I need to find every real-I/O boundary. Let me map them.

Good — the MySQL boundary is a single choke point: `pymysql.connect` in `app/shared/db_mysql.py:133` (both `pool` and `log_pool` go through it). Let me see how the DB-layer test handles it (so a guard won't break it) and check the DeepSeek/CRM boundaries.

`test_db_mysql.py` is immune to a `pymysql.connect` guard — it patches `_connect_once`/`connect_with_retry` on its own throwaway instances and never calls the real connector. That means I can use `pymysql.connect` as a single global tripwire. Let me check the CRM/LLM HTTP boundaries and how timeline/reason tests fake them, so I cover those too.

I now have the full isolation picture:
- **MySQL**: single boundary — `pymysql.connect` (`db_mysql.py:133`); both `pool` and `log_pool` go through it. `test_db_mysql.py` patches *above* it, so a guard there won't break it.
- **CRM/LLM**: tests fake at the service/client level (`timeline_service.get_timeline`, `recommendation_service.get_recommend_reason`, `timeline_service.crm_client`, `FakeLLM`, churn patches `crm_api.httpx.post`). Real `httpx` is never called — and I *can't* blanket-guard `httpx` anyway because TestClient itself runs on it.

So the definitive guard for "no test touches a real DB" is a `pymysql.connect` tripwire. Let me create a root conftest with that tripwire **plus** the api-log silencer (needed separately because the middleware writes from a background thread, which a per-test DB patch can't reliably catch).

Now the moment of truth — run the full suite. If it stays green with the `pymysql.connect` tripwire armed, that's proof no test touches a real DB.

527 green with the `pymysql.connect` tripwire armed — that's the proof: **no test in the suite (lead, churn, or shared) opens a real MySQL connection.** (The churn `mysql_repo` tests run on in-memory SQLite via SQLAlchemy, so they're hermetic too — that's the harmless sqlite deprecation warning.)

Let me do one sanity check that the tripwire actually *fires* (so "green" means "armed and not tripped," not "no-op guard").

Both guards proven live, temp file gone. Let me lint/format the new file and confirm exactly what changed.

Done. Both guards are in, the suite is green with the tripwire **armed**, and I proved the tripwire actually fires.

## 结果

| 检查 | 结果 |
|---|---|
| 全量 `pytest -q` | **527 passed**(守卫全程武装) |
| MySQL tripwire 自检 | 真调 `pymysql.connect` → 立即 `RuntimeError: TEST GUARD` ✅(证明不是空守卫) |
| api 日志默认关 | `enable_api_logging is False` ✅ |
| ruff format + check | All checks passed ✅ |
| 本次改动 | 仅新增 `tests/conftest.py` 一个文件 |

## 加了什么(`tests/conftest.py`,两个 autouse 守卫)

1. **`_block_real_mysql`**——把真连库的唯一边界 `pymysql.connect` 焊成"一调就炸"。业务库 `pool`、`log_pool`、churn 的 SQLAlchemy(经 pymysql 驱动)全走它。**527 在它武装下依然全绿 = 全套测试(lead/churn/shared)没有任何一个真的连过库**(读或写)。这就是你要的"确认其他测试不写真实数据库"——而且是证明级的,不是口头保证。

2. **`_silence_api_log`**——把 `ApiLogMiddleware` 默认关掉 + 写库函数桩成 no-op。这是你同意加的那个 fixture,我放在**根 conftest**(覆盖全套,不只是 lead),`tests/lead/conftest.py` 的"opt-in、不 autouse"设计原样不动。专测日志的 `test_api_log_*` 在各自用例里自己打开、自己桩 `insert_logs`,覆盖这个默认,所以不受影响(已验证全绿)。

> 为什么要两个分开:api 日志是在**后台线程异步落库**的,#1 那种 per-test 的 DB 守卫在 monkeypatch 还原后可能兜不住(flusher 晚一拍才写),所以必须在源头(#2)关掉,不能只靠 #1。

## 一个诚实的边界

- **数据库:已证明**(tripwire 武装下 527 全绿)。
- **CRM / 大模型(HTTP):靠代码审查确认**,不是硬守卫——测试都在 service/client 层注入假件(`timeline_service.get_timeline`、`recommendation_service.get_recommend_reason`、`timeline_service.crm_client=_OkCRM()`、`FakeLLM`、churn 桩 `crm_api.httpx.post`),真 `httpx` 不会被调。**没法给 httpx 加全局 tripwire,因为 TestClient 本身就跑在 httpx 上**,一拦它自己就废了。要的话我可以再给那几个具体的 app client 对象(crm_client/deepseek_client)加针对性 tripwire,把"无外部依赖"也焊到 HTTP 这层。

## 还在你那边的两件事

1. **清掉已漏的 92 行**(生产库,我碰不到):`DELETE FROM lead_api_log WHERE id BETWEEN 5417 AND 5508;`
2. **别在服务器、带生产 `.env`(`ENABLE_API_LOGGING=1`)跑 pytest**——现在加了守卫,即便误跑也不会再漏写(中间件被默认关掉),但养成"测试用干净环境、生产验证用 `make smoke`"的习惯最稳。

要不要我把这事记进项目记忆(下次部署/有人在服务器跑测试时能避免重蹈)?也可以顺手把 HTTP 那层 tripwire 一起补上。

---

### Turn 10  *(2026-06-17 09:44)*

**User:**

别在服务器、带生产 .env(ENABLE_API_LOGGING=1)跑 pytest，这是什么意思？现在测试日志不写进真实数据库了吧，真实调用还是会进的吧

**Assistant:**

你的理解**完全正确**,而且我那句"别跑 pytest"现在已经降级成"习惯"而非"硬规定"了。逐条说清楚。

## 先回答你最担心的:生产日志我一个字都没动

| | 真库会被写吗 | 谁控制 |
|---|---|---|
| **真实调用**(业务员 app → nginx:8080 → docker 里的 uvicorn) | **会,照常写** ✅ | 生产 `.env` 的 `ENABLE_API_LOGGING=1` |
| **pytest 测试**(TestClient 模拟请求) | **不会** ✅(守卫关掉了) | `tests/conftest.py` 的守卫 |

**为什么两者井水不犯河水**:我加的守卫只在 `tests/conftest.py` 里,**只有你敲 `pytest` 时才会被加载**。生产环境跑的是 docker 里的 uvicorn(`app.main:app`),它**根本不读 `tests/` 目录**——守卫对它不存在。所以生产 app 该记日志继续记,`ENABLE_API_LOGGING=1` 照常生效。

举个具体例子:业务员在 app 上点一次"推荐理由" → docker 里的 app 处理 → `lead_api_log` 多一行。**这条链路我完全没碰**。你要的功能(问题一)还在。

## "别在服务器带生产 .env 跑 pytest"是什么意思

`pytest` 是**跑测试套件**这个动作。那 92 行的来历就是:有人在服务器上敲了 `python3 -m pytest`,而测试套件内部会用 TestClient **模拟成百上千次接口调用**(边界值、注入串那些)。修复前,这些**模拟**调用被中间件当成真流量记进了生产 `lead_api_log`——这就是"漏写"。

我当时那句话是**修复前的注意事项**。现在加了守卫之后:

> **即便你真在服务器上、带生产 `.env` 跑了 pytest,也不会再漏写了**——守卫会把日志默认关掉、并拦死真库连接。

所以这条从"**绝对不能**"降成了"**习惯上别这么做**":测试套件本来就该在开发机 / CI 上跑,不该拿生产服务器和生产库当试验场(跟"漏不漏写"无关,纯粹是工程卫生)。部署后想验证服务活没活,用 `make smoke`(只打 1 次真实 score-and-info,不模拟一堆垃圾)就够了。

---

一句话总结:**测试 = 不碰真库(已焊死);真实业务请求 = 照常记日志(没动)。** 你剩下要做的还是那一件——把已经漏进去的 92 行删掉:`DELETE FROM lead_api_log WHERE id BETWEEN 5417 AND 5508;`

(上次问你的两件事——记进项目记忆 / 给 CRM·大模型那层也补 tripwire——你要的话我随时做;不要也不影响现在的修复。)

---

### Turn 11  *(2026-06-17 09:48)*

**User:**

给 CRM·大模型那层也补 tripwire，这是什么意思

**Assistant:**

"补 tripwire" 就是把我给 MySQL 做的那套"一碰真东西就大声炸"的守卫,照搬到 CRM 和大模型这两条**对外调用**上。意思拆开讲:

## tripwire 是什么(回顾)

就是测试里的一道"绊线":一旦某个测试**真的对外发起调用**,立刻 `RuntimeError` 炸掉,而不是悄悄把请求发出去。MySQL 那条绊线是 `pymysql.connect`——527 在它武装下全绿,就证明了没有任何测试真连过库。

## CRM / 大模型这两条线指什么

你的 lead 有两个**往外网打**的客户端:
- **CRM**(`app/lead/clients/crm.py` 的 `crm_client`):timeline 取订单/拜访/服务,真打 `crm.amer.cn`。
- **大模型**(`app/lead/clients/deepseek.py` 的 `deepseek_client`):recommend-reason 调 DeepSeek,真打 `api.deepseek.com`——**这个是要花 token、花钱的**。

现在这两条**没有硬绊线**。我上次说"它们不写真库"是我**逐个读测试确认的**(测试都注入了假件:`timeline_service.crm_client=_OkCRM()`、`FakeLLM`、桩 `get_timeline`/`get_recommend_reason`)。是"我看过、没问题",不是"焊死了、不可能"。

## 为什么不能跟 MySQL 一样一把全局拦

- MySQL 只有 `pymysql.connect` 一个口,且**只有 app 用它** → 全局拦掉很安全。
- HTTP 不行:`httpx` 这个库**app 的客户端在用,FastAPI 的 TestClient 自己也在用**(TestClient 就是靠 httpx 在进程内驱动你的 app)。我要是把 httpx 全局拦了,**测试里每一次 `client.post(...)` 自己先炸**,整套测试全废。

所以 CRM/大模型的绊线得**精准打在那两个客户端对象的"对外发请求"方法上**,绕开 TestClient 的 httpx。

## 补上之后能多换来什么

- 把"我读过、确认没漏"升级成"**焊死 + 一次绿跑证明**":跟 MySQL 一样,加完跑 `pytest`,还是 527 全绿,就等于证明了没有任何测试真打过 CRM / DeepSeek。
- **防将来**:哪天有人新写个测试忘了桩 CRM 或大模型,它会在 CI 里**当场炸**,而不是悄悄去读生产客户数据 / 悄悄烧 DeepSeek 的钱。对大模型这条尤其值——真漏调比漏写日志更肉疼(花钱、还不确定性)。

## 具体怎么做(很小,纯测试改动)

1. 看一眼 `crm_client` / `deepseek_client` 上真正发请求的那个方法名;
2. 在 `tests/conftest.py` 再加一个 autouse fixture,把这俩方法 monkeypatch 成"一调就抛 TEST GUARD";
3. 跑全量 `pytest`——�Green则证明完成。现有那些在 service 层 / 注入假 client 的测试根本到不了真单例,不受影响(我会跑一遍确认)。

要我现在就加吗?加的话顺带把 MySQL 那条的同款注释也给这条写清楚。

---

### Turn 12  *(2026-06-17 09:52)*

**User:**

有必要吗

**Assistant:**

直接说判断:**不是必须,但我倾向加**——理由很窄,就一条值钱的。

**为什么现在不慌**:没有任何当前测试会漏调 CRM/大模型,我逐个读过都桩了假件,527 也绿。今天没病。

**为什么仍倾向加**:你已经把"**在生产服务器误跑 pytest**"这个坑堵了**一半**——DB/日志那半焊死了。CRM/大模型这半现在还只靠"我读过没问题"撑着。而这半里,**DeepSeek 漏调是要烧 token、烧钱的**(生产 `.env` 有 `DEEPSEEK_API_KEY`,真在那跑 pytest 又恰好有测试没桩,就真花钱了)。要么就别留这半口子、跟 DB 一样焊死;要么就接受"它没硬保证、靠纪律 + code review"。

**我的取舍**:
- 加 → 便宜(一个 fixture)、把同一个坑堵全、护住 DeepSeek 的钱,且加完一次绿跑就把"我读过"升级成"证明过"。
- 不加 → 也不算留病,前提是你守住"测试不在生产服务器跑、新测试都桩掉外部调用"这两条纪律。

说白了:**DB 那条是救火(真出过事),这条是买保险(没出过事、但漏了更肉疼)。** 保险不贵,我会买;但你要是想保持测试基建最小、靠纪律兜,我也不反对。

你定:加,还是到此为止?

---
