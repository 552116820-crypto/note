# 本周工作日志（按项目划分 · 2026-06-08 ~ 06-13）

## 1. 客探二期 · OGSM 拆解与规划
- 拆解上级分派的客探二期技术实现，落为「流失预警」「智能拓客」两个目标，逐一填写 S 策略、M 衡量指标、A 行动计划（每个策略一一对应自己的指标与计划），已提交等待审核。`90% 待审核`

## 2. 客探二期 · 智能拓客（接口开发与联调）
- /score-and-info 接口支持按行业筛选，请求体新增 industry_ids / industry_names（均可选，id 优先）；提供行业 id 与名称对应关系接口给前端，规范行业名称（传 industry_names 时反查 industry_ids）。`100%`
- AI 推荐接口做好大模型兜底策略，按确定性模板输出。`100%`
- 对接前端工程师，展示接口返回模板，约定 message 字段成功返回为空串。`100%`
- 接口边界测试、压力测试，编写接口文档。`50%`（待 nginx 反代域名后再测）

## 3. 客探二期 · 流失预警（精简与重构）
- 删减冗余代码：早期因对接不充分、需求不清、职责不明，做成了完整后端系统；现仅保留检测算法与 CRM 推荐链路。`50%`
- churn 模块重构，删除退役代码，仅保留「检测预警→推送 CRM 数据库」一条链路，不再提供完整服务接口；拆解大脚本文件，代码完全贴合业务逻辑。`80%`

## 4. 客探二期 · 项目合并（流失预警 + 智能拓客）
- 合并前准备：对两项目做代码审查 + 全量冒烟测试。`50% → 100%`
- 机械并入两个项目代码。`50%`
- 合并项目共享层去重，搭建脚手架与规范开发文档；shared 层迁移非共享代码，结构清晰无冗余。`80%`
- 审查并规范合并后的项目结构。`80%`
- 文档更新，同步合并改动，将技术栈、技术细节、方法思路写入文档，便于交接与后续模块化开发。`80%`

## 5. 部署与运维（Docker / 网络 / CORS）
- 准备 Docker 部署文件：Dockerfile、docker-compose.yml、.dockerignore，重点关注 Redis 服务。`80%`（待上服务器确认 url 与端口）
- 给前端本地测试接口：解决连接问题，解开 CORS 限制，防火墙放行端口、放行外部进入 WSL，建 docker-compose.override.yml，用 IP 供局域网前端同事访问。`80%`

## 6. 代码质量与性能优化
- 审查代码，排查并修复内存泄露，改成 striped lock；优化 CRM 接口串行请求，改成模块级有界池并行调用。`30%`（待两项目合并后进一步优化）
- 学习 ruff（Python 代码检测工具）、mypy，结合 AI 方式审查代码（更高效、更有信服力）；深入 striped lock 池、模块级有界池的高阶使用与项目应用。`50% → 70%`

## 7. 企微机器人 / Dify / 滴普智能体集成
- 排查 dify 客探工作流报错并优化：名下客户为 0 时默认查公海，避免 dify 更新文档接口无内容报错。`100%`
- 排查滴普企微机器人回复异常，优化单气泡窗口字节上限设到 20480，尽量单条消息装下全部内容。`100%`
- 服务器压测企微机器人桥接滴普智能体服务：最大并发 32；48、64 时出现滴普网关侧排队签名超时，达到网关并发连接数上限。`50% → 100%`
- 了解企微机器人编辑共享文档的限制；调研基于 openclaw 的实现方式，插件与 mcp 两种较易实现。`80%`

## 8. 技术学习与调研
- DMD 分布匹配蒸馏：用于加速生成模型压缩与蒸馏，只对最终图像质量负责，广泛用于 AIGC 实时生成。`100%`
- GraphRAG 图谱 RAG：建立实体关系与属性，提升模型置信度与证据量，降低更新维护成本（图谱本质是三元组关系）。`100%`
- Claude Agent SDK：Anthropic 面向 agent 开发者的框架库，可实现与 claude code 同样的架构，为后续 agent 搭建做准备。`20%`
- 验证 Claude Agent SDK 使用限制：非原生 claude 模型 API 不能完全兼容，且 SDK 的 harness 是为 claude 模型定制。`100%`

## 9. 团队协调与资源跟进
- 参加 AI 团队每周例会，讨论后续工作计划、未来工作安排与工时预估。
- 联系 AI 工具会员负责人：提醒 claude max 会员已过期需续费或新开账号；银河云 vpn 不稳定/不可用，已联系供应商解决。`50%`
